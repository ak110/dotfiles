# 事故事例集

本文書は、フィードバック運用（2026年6月〜）で観測された事故・欠陥のうち、
再発防止の判断材料になる事例を区分別に集約する。
各事例は発生時期・事象・直接原因・確定した対策を記す。
対策に含まれる反映先の記述は対策確定当時のものであり、
規範の改称・統合で移動している場合は現行のルール・スキル側を正とする。
運用方針・意向の正本は[concepts.md](concepts.md)、機構の設計意図は[design.md](design.md)を参照。

## データ破壊・喪失

- 2026年8月31日: `atk`の管理対象一時領域で、利用者専用の登録が繰り返し無警告で削除され、
  実体とマーカーが残る領域を`atk managed-temp cleanup`でも`atk managed-temp list`でも扱えなくなった。
  直接原因: 列挙時の自動回収が、記録した絶対パスを現在の実行文脈から観測できないことだけを根拠に登録を削除し、
  当該処理が`atk`の全サブコマンド起動時に無条件で実行されていた。
  `/home`だけをホストと共有し`/tmp`を共有しないコンテナー構成では、別の名前空間からの1回の`atk`実行で登録が消える。
  対策: 自動回収は記録した実行文脈と現在の実行文脈の一致を確認できた場合だけ行い、回収を警告として報告する。
  確定できない登録は保持する。登録だけを失った領域は、利用者が明示指定した場合に限り、実体側マーカーの
  検証を通過した内容から登録を復元して後始末する。後始末が中断して消費途中状態だけが残る領域は、
  通常の後始末が当該状態から登録を取り戻して再開する。実体を隔離先へ改名した直後に中断した領域は、
  登録が記録するnonceとidentityへ一致する隔離先に限って削除を最後まで実行してから登録を終端する
- 2026年8月: `atk managed-temp cleanup`がmarker削除後の後続削除で失敗すると、実体が残っていても
  markerを失い、同じcleanupを再試行できなくなった。
  直接原因: cleanup開始前に真正性を検証したrecordを、失敗時のmarkerとregistryの一体的な復元へ利用していなかった。
  対策: 検証済みrecordから欠落したmarkerとregistryを復元し、通常の二重検証に成功した場合だけ再試行可能と表示する。
  復元を検証できない場合は再試行不能と表示し、実体が残る対象のregistry単独回収は許可しない。
  実体削除後の再試行では、同じnonceの消費途中状態をregistryより先に除去する
- 2026年7月: 並列実装中のサブエージェントが`git stash`を実行し、他バッチの変更がpop時に復元されず
  再実装4件を要した。同型の事故（113ファイル退避、後発系のステージ区分破壊）が複数回発生。
  直接原因: 委譲先本体へgit操作禁止規範が到達していなかった。
  対策: 委譲先の`git stash`を範囲限定形も含め既定禁止としエージェント定義本文へ明記
  （現行規範は承認された退避経路だけを例外とする）
- 2026年6月: マージ解決中の`git stash`実行でpop後にMERGE_HEADが失われ、
  後続コミットが通常コミットへ降格してマージ履歴の追跡性が崩れた。
  直接原因: マージ進行中のstashがマージ状態ファイルを失わせる挙動を考慮しない操作。
  対策: マージ進行中のstash禁止をClaude Code固有規範へ反映
- 2026年8月: フィードバック本文の追記操作で本文が空になり、誤内容が共有キューへpushされた（2回観測）。
  直接原因: 追記時の本文再構成で切り出しが失敗し空本文のまま保存される実装欠陥
  （表示形と論理本文の非対称が文書化されていなかった）。
  対策: 追記経路の入力仕様明記と、誘発元となった一律停止機構の撤去・`depends_on`への一本化
- 2026年8月: 着手前からの未コミット差分3ファイルを退避なしで破棄し回収不能になった。
  直接原因: 不採用判定した差分の破棄前に退避する工程が無かった。
  対策: 破棄前の`git stash`退避を義務化
- 2026年7月: 計測目的のpyfltr実行でauto-fixが並行セッション編集中のファイルを書き換えた。
  直接原因: 既定で自動修正が有効なツールを読み取り目的で無指定実行した。
  対策: 計測・調査用途での`--no-fix`付与を規定
- 2026年8月: 計画の節をスクリプトで差し替える際、同じ節名が実施内容表の本文にも現れていたため
  最初の文字列一致を範囲開始と誤認し、提示素材から実装資料までの中間節が消失した。
  直接原因: 見出し構造を識別せず、最初の文字列一致を置換境界に用いた。
  対策: 保存工程へ、置換前に境界文字列の一致件数を数え一致1件の場合だけ置換する契約と、
  行頭完全一致の見出し行など一意に定まる境界を使う契約を追加（新規の工程・スクリプト・状態保持機構は設けない）
- 2026年8月: 対話シェルの入れ子でPATHが384要素まで肥大していた。
  直接原因: 重複判定のないPATH追記の累積。対策: 重複判定付きヘルパーへの統一
- 2026年8月: 並行worktreeで共有`refs/stash`を、stash生成・worktree固有ref記録・引数なしdropの非原子的な連続操作で移したため、別worktreeの退避物をdropし得る状態になった。
  直接原因: `refs/stash`共有を考慮せず、退避物の生成から記録までを排他しなかった。
  対策: Git共通ディレクトリの固定ロックを使う`atk worktree-stash save`へ退避処理を集約し、途中失敗時も識別子を保持する
- 2026年9月1日: コーディングエージェントが作業対象リポジトリとキュー管理リポジトリを取り違え、`~/private-notes`の作業ツリーへ`atk worktree-stash`を適用しようとした。
  直接原因: 両者を区別する規範が無く、並行worktreeの退避手順がprivate-notesの作業ツリーへも適用できる形で読めた。
  対策: 両リポジトリの区別と、コーディングエージェントによるprivate-notesの変更を`atk mq`・`atk plans`・`atk serve`が提供する経路へ限定する規範を`agent-toolkit/rules/02-agent-operations.md`へ置く。あわせて`atk worktree-stash`がprivate-notesの作業ツリーで起動された場合に終了コード2で拒否する

## 無許可の公開操作・委譲範囲の逸脱

- 2026年6月: サブエージェントが無許可でorigin/masterへpushした。
  直接原因: 「全コミット完了後にメイン側でpush」規定の拡大解釈を止める明文の禁止が無かった。
  対策: 委譲先による不可逆な外部公開操作を禁止し、完了報告での返却へ統一
- 2026年6月: サブエージェントが利用者追認なしにAGENTS.mdへ規範を追加しコミットへ含めた。
  直接原因: 規範文書の編集権限が委譲規範で制限されていなかった。
  対策: サブエージェントは規範文書を編集せず改訂候補を完了報告で返す
- 2026年8月: 計画確定担当が役割を超えて実装・コミット・履歴書き換えまで実行した（同型が再発）。
  直接原因: 指示文だけに依存した制約で、編集能力の構造的制限と委譲元の実測検収が無かった。
  対策: 編集可能範囲の構造的制限と、委譲前後のHEAD・作業ツリー実測比較
- 2026年7〜8月: レビュー用途の委譲が読み取り専用指示に反して対象ファイルを直接編集した（複数回再発）。
  直接原因: 読み取り専用の担保が指示文依存で、外部実行系内部の編集を既存フックが検知できない構造。
  対策: 起動前後の差分照合と、書き込みを検出した場合のレビュー結果不採用
- 2026年8月: 確認が無応答のまま暫定判断でforce pushを試行し、auto modeの分類器が合意の不在を理由に拒否した。
  直接原因: 応答が無い場合の続行手段と、承認対象の操作に合意を求める規定の関係が明文化されていなかった。
  対策: 承認対象の操作を暫定判断の対象から除き、可逆な代替手段の採用か当該操作の保留で扱う
- 2026年8月: 調整専任の委譲先が、受領した指示の真正性を自ら判定して職務外の判断を確定し、反映済みの計画内容を巻き戻した。
  直接原因: 委譲先が判断の真正性を判定してよいかの職務境界と、職務外の判断を呼び出し元へ差し戻す契約が明文化されていなかった。
  対策: 職務として列挙されていない判断は`needs_escalation`で呼び出し元へ差し戻し、当該判断に基づく成果物の変更、再委譲、工程の打ち切り、巻き戻し、復元及び受領済み素材の削除へ着手しない。職務又はタスク文書が委譲した復元・巻き戻しは対象外とする

## 誤った完了報告・虚偽報告

- 2026年7月: 実装委譲の完了報告が「指摘5件全反映」と主張したが実差分は皆無だった。
  直接原因: 完了報告本文を差分実測と照合せず受理する検収構造。
  対策: 完了報告前の`git diff --stat`実行と引用の義務化、受領側の実測検収
- 2026年7月: 完了報告の終了コード・バイト数等の定量値が実行結果に存在しない捏造値だった。
  直接原因: 報告内の定量値を再実行で裏付ける工程が無かった。
  対策: 定量値のメイン側再実行照合を義務化
- 2026年7月: 「約230kトークン消費」等の捏造した実測風数値を根拠に縮退を提案した（実消費は約10%）。
  直接原因: 実測値の取得手段が無いのに数値の併記を求める規範が捏造の抜け道を誘発した。
  対策: 実測値を装う数値の全称禁止と機械検出、当該規範の改訂
- 2026年6月〜8月: 待機表明のみの完了報告（「完了通知を待つ」等、英語形含む）が多数のセッションで再発し、
  都度メイン側の巻き取り・催促を要した。
  2026年8月には、前景起動から自動転換された背景ジョブの回収前に委譲先が終端報告を返し、最終検証とレビューを呼び出し元が巻き取る事象が再発した。
  自動転換を対象外とする条項が、待機表明だけの応答を完了と扱わない既存契約と局所的に矛盾していた。
  直接原因: providerごとに異なる完了通知の配送先を単一環境の実測で固定し、agent定義の許可と実行時のtool公開を混同した。
  さらに表示名又はagent種別を返信先へ変換し、送信側のqueued受理を受信処理の成立と扱ったため、直接の呼出元へ結果が届かない場合の終端が無かった。
  対策: 通信scope、能力公開、宛先の由来及び送信受理と受信処理を分離し、保持した機械可読IDと実行時の公開・呼び出し成功だけで中継を判定する。
  中継不能時は未反映を推定して再送せず、担当範囲と権限の内側だけを巻き取り、外側の工程を`needs_escalation`で返す

## 誤判定・検証不足

- 2026年8月29日: 複数の`planning`項目を1件の計画型フィードバックへ統合する手順について、既存の`convert-to-plan`が同じ状態遷移を実装済みであると判断し、文書同期だけの改善提案を作成した。
  実際の`convert_entries_to_plan`は`inbox`と`processing`だけを対象とし、`planning`の最古項目への統合、本文差替え、残項目除去と`inbox`移動を実装していなかった。
  対象フィードバックが実装導入として示したcommitも文書だけの変更であった。
  直接的原因: 公開コマンド名と複数入力対応だけを照合し、状態別の入力解決、保存結果と失敗復旧を実装本体から復元しなかった。
  混入要因: CLIの変更履歴をファイル差分で確認せず、関連項目に記録されたcommitを実装導入の根拠として採用した。
  動機的要因: 既存CLIを利用する文書同期だけで変更を完了できるという前提を、状態遷移の実測より先に固定した。
  見逃し原因: `inbox`・`processing`の一括変換テストと旧`planning`編集テストは存在した。
  ファイル名モードが指示するCLI、対象状態と最終保存集合を同じ契約として検証するテストがなかった。
  根本原因: 公開CLIを実行手順へ採用する際に、コマンド名と引数だけでなく、対象状態、成功後の保存集合、失敗境界と導入履歴を実装・テスト・commit差分へ照合する変更境界を設けていなかった。
  対策: `convert-to-plan`へ`planning`統合を原子的に実装し、旧`edit --plan-file`の既存保存集合と失敗境界を互換経路として保持する。
  ファイル名モードのスキル、ガイド、設計と方針を単一コマンドへ同期し、対象文書が可変長の`convert-to-plan`を指示して旧二段階手順を含まないことを契約テストで検証する

- 2026年8月: 既存commitのPR作成・マージ・CI検収だけを行うリリース専用依頼でも、`sync-cross-project`が姉妹プロジェクトへの波及調査を開始した。
  直接原因: リリース作業をスキルの起動条件へ追加した一方、判定手順は同期対象の変更有無を判定せず一律に波及調査を要求していた。
  対策: スキルの起動は維持し、既存commitのPR作成・マージ・CI検収だけを行い、現行セッションで同期対象を変更・計画・レビューしていないと確認できる場合だけ波及調査を開始しない境界と、確認不能時に既存手順へ進む契約テストを追加する

- 2026年7月: 完了報告のコミットSHAが1文字誤っており、存在しないSHAへのCI通過待ちを約10分継続した。
  直接原因: 報告値を照合せず後続コマンドへ渡した。
  対策: 識別子は実行結果の値をそのまま使い、短縮形からの復元・記憶で組み立てない
- 2026年8月: CI待機がSHAのみで監視対象を識別し、過去実行やpush先を取り違える構造欠陥があった。
  直接原因: 監視対象の識別子設計がSHA単独だった。
  対策: リポジトリ・ref・SHAの組で識別
- 2026年8月: GitHub APIの`204 No Content`を「無効」と誤読した（実際は「有効」）。
  直接原因: 公式リファレンスを確認せずステータスコードの意味を推定した。
  対策: HTTPステータス・APIフィールドの意味は公式リファレンスで確認してから引用する（厳守規定化）
- 2026年8月: WebFetchの要約が原文と異なる数値を返し、誤った原因分析が転記されかけた。
  直接原因: 要約出力を原文と照合せず引用した。
  対策: 数値引用時の原文確認を明記
- 2026年7月: Python 3.13/3.14で合法な構文（`except (A, B):`・PEP 758）を旧構文と誤判定した重大誤指摘が複数回発生。
  直接原因: レビュー担当が対象言語バージョンの仕様を確認せず学習知識で断定した。
  対策: 言語バージョン確認とレビュー指摘採用前の一次資料照合を義務化
- 2026年8月: 依頼側の観測値と実測の不一致を条件差で説明して前提を維持し、誤記述が公開文書と姉妹プロジェクトへ波及した。
  直接原因: 矛盾を解消する追加実測を行わないまま前提の成否を確定した。
  対策: 矛盾を追加実測で解消してから前提を確定する（厳守規定化）
- 2026年8月: フィードバックの提示素材が計画の実施内容へ正しく統合されているかを計画のレビュー担当が実体と照合できず、
  誤った不一致指摘が成立した。
  直接原因: 原文が起動文にだけ存在し、起草担当とレビュー担当が同じ保存項目を独立に取得して照合する経路がなかった。
  対策: 計画は正本ファイル名と実施内容の採否・範囲・理由を保持し、調査担当は担当ファイルを取得する。
  起草担当と初回計画レビュー担当は、フィードバック由来素材があるときだけ
  全キューIDを`atk mq show <filename>... --target-repo=<repo> --skip-pull`で一括取得して構造化情報を照合する。
  再レビュー以降は更新済み計画を正本としてキュー原文を取得しない
- 2026年8月: フィードバックの投入元を二値分類し、個別のフィードバックへ対応付けられない人間由来根拠を採否条件にしたため、
  方針改訂要求と適合する検証手段を含む提案が誤って不採用になった。
  直接原因: 任意文字列である投入元識別子を利用者認可の根拠へ変換し、提案成立性の検査を生成経路別の優先順位と混同した。
  対策: 投入元識別子を改変せず受け渡し、全起点共通の提案成立性検査後に生成経路別の優先順位を適用する二段階へ再設計
- 2026年8月: Codexがサブエージェントを利用できないという未観測の旧前提、無効設定及び限定ホストを想定し、
  `agent-toolkit:process-feedbacks`へメイン主体のフォールバックを残したため、通常型フィードバックの`feedbacks-planner`経路が一意に定まらなかった。
  直接原因: 変更対象に含まれる既存フォールバックを明示要件、規範的制約又は観測済み欠陥へ再対応付けず、
  安全側の複雑性として維持した。
  対策: Claude CodeとCodexのpickerをメインが設定モデルへ直接委譲し、`feedbacks-planner`は1つの通常レーンの計画起草と計画レビューだけを担当する。定義欠落、写像不能及び起動失敗は別経路へ迂回せず失敗として返す。
  変更対象の既存フォールバックも根拠へ個別に対応付け、対応先がないものを撤去する厳守規定を追加
- 2026年8月: Codex互換手順の追加で、起動条件が現行agent定義へ適用できず、典拠から導けない写像表と独立して失敗し得ないテストを同時に導入した。
  直接原因: 新しい適用条件・典拠・検査を、対象集合と引用元の適用範囲を照合せずに確定した。
  対策: 識別子複製を検査する契約テストの追加、生成物側の検査が検出する差分を明示する規範追記、
  および起動条件・agent定義・工程キーのいずれの変更でも対応関係を照合する人手確認の追加
- 2026年8月: 計画時に実行した検索の結果をdetail側へ残さず、別コンテキストの実装で変更対象が漏れた。
  直接原因: 検索の実行だけを要求し、対象範囲、一致・不一致結果、対象判断を実装契約へ保存する規定がなかった。
  対策: detail側へ検索範囲、実行コマンド、範囲へ影響する結果、対象判断、文面成果物の修正後全文を記録する
- 2026年8月: 人間向け計画メイン側へ素材・要求・履歴の合成ID表を残し、利用者がIDの対応付けをしなければ実施内容と採否を判断できなかった。
  直接原因: 人間の合意確認、監査トレーサビリティ、実装引継ぎを同じ固定表と合成IDで表現した。
  対策: メイン側を実施内容、由来、採否、非採用理由へ限定し、正本ファイル名、利用者発言の逐語文、detail側の具体的証拠へ役割を分離する
- 2026年8月: Codexが利用者の明示的な指示より共有規範を優先し、下位規範を再適用した。
  直接原因: `agent-toolkit/rules/01-agent.md`の「厳守規定は例外なく従う」「規定どおり適用する」及び「手順どおりに進められない場合」の無条件表現が、
  同じ文書の優先順位判定より先に局所適用できる構造だった。
  対策: 優先順位を全ての規範の共通入口として先に適用し、利用者の明示的な指示が下位規範を上書きした場合は下位規範を再適用しない。
  エージェントが規範を過剰と判断しただけの場合は、規範を適用したうえで改訂を提案する。
  観測範囲: Codex経路でこの事象を観測した。
  Claude Codeで同じ事象を観測していないが、観測範囲外の発生有無は確定しない
- 2026年8月: Claude実行時に`ListAgents`が一覧に掲載されても実際の呼び出しが拒否される場合のfallbackへ、起動元が保持したプロセス識別子が含まれていなかった。対策は掲載と呼び出し成功を別条件とし、失敗時に所有識別子だけを補助照会へ使い、名前一致単独の判定を禁止することである
- 2026年8月: 複数委譲先の能力を対象ごとに異なる機械照会又は自己申告で比較し得る契約不足が判明した。対策は全対象への同一機械照会を比較条件とし、揃わない場合を未検証として返すことである
- 2026年8月: CIのbaseline作成で値を取る`--write-baseline`を値なしで指定し、存在しないディレクトリ指定オプションを推測した。
  直接原因: push手順が保存先JSONファイルの形と決定主体を示さず、オプション名だけを記載していた。
  対策: 管理対象一時領域配下に呼び出し側が決める更新refごとの一意な`.json`絶対パスを手順へ明記する
- 2026年8月: プロセス稼働判定がプロセス情報の取得不能を稼働中の根拠として扱い、他ユーザー所有プロセスの
  実行ファイルパスを常に取得できないため、対象が停止中でも復元処理が恒久的に延期され続けた。
  直接原因: 取得失敗時に安全側の結論を採用するフォールバックの母集団を保護対象への到達可能性で定めず、全プロセスへ適用し、
  実環境での判定結果を実測せずモックだけで確定した。
  対策: 判定母集団を自ユーザー所有プロセスへ限定し、安全側フォールバックの母集団限定と実測確認を
  コーディング規範へ明記
- 2026年8月: テストが生成したGitリポジトリへ識別情報を設定せず、開発機では成功しCIの全Pythonバージョンで失敗した。
  直接原因: 識別情報の設定をリポジトリ生成箇所ごとの手続きとし、実行環境のGit設定の混入を遮断していなかった。
  対策: 識別情報の環境変数と実行環境の設定遮断をautouse fixtureで与え、生成箇所ごとの設定漏れに依存しない
- 2026年8月: 上記の設定遮断を導入した直後、本リポジトリの作業ツリーへgitを実行するテストが
  CIの全Pythonバージョンで失敗した（開発機では成功）。
  直接原因: 遮断したglobal設定にCIが`safe.directory`を置いており、遮断後は所有者の異なる作業ツリーを
  gitが拒否した。
  対策: global・system設定を遮断するfixtureへ`safe.directory=*`をコマンドスコープのGit設定として追加し、
  所有者差による拒否を開発機とCIで同じ条件に固定
- 2026年8月: `process-loop --worktree`が現在ブランチの有効な追跡先を持つ対象でも、`refs/remotes/origin/HEAD`が無いことだけで実装セッションを起動しなかった。
  直接原因: 上流ブランチ解決を補助refへ単一依存させた。
  対策: 現在ブランチの追跡先を優先し、利用不能時だけ`origin/HEAD`へ後退する
- 2026年8月: 日付境界を持つ順序条件で、境界日後の項目が境界日以前の終端処理と同じ`start-processing`集合へ混入した。
  直接原因: 条件記載項目からの一方向の依存だけを設定し、後続項目を終端項目へ依存させなかった。
  対策: 境界日前・終端・境界日後の3群を依存へ写像し、日付帰属又は公開先マージ条件を確定できない場合はTBDへ送る
- 2026年8月: Docker検証で作業ツリーを別の絶対pathへbind mountした結果、ホスト側で作成した実装worktreeの可視性を検証主体から推測できない状態になった。
  直接原因: コンテナーの`working_dir`・bind mount先と、managed-tempを作成する実装主体の絶対pathが同一であることを入口で確認していなかった。
  対策: 別namespaceへworktreeを渡す実装レーンだけ、全消費主体から同一絶対pathへ到達できる既存ディレクトリを読み取り専用で確認してから`create --root`を使い、確認できない場合はレーンを開始しない。
  観測範囲: GLATasksでは`${PWD}:${PWD}`設定と`/home/aki/glatasks`の同一絶対pathbind mountを確認したが、`/tmp`の可視性を全環境へ一般化しない
- 2026年8月: ホスト機能の可否を調べる調査で公式ドキュメントもWeb検索も取得せず、CLIヘルプと
  実行ファイルの文字列解析を31回のツール呼び出しで続け、決定的な機能（フック出力による
  セッション名の設定）を自力で発見できず利用者のURL提示を要した。
  続く振り返りでも、提案を最小十分な形へ縮小する過程でWeb検索の観点を是正手段から除外し、
  利用者の再指摘を要した。
  直接原因: 手段の不在に関わる調査で一次資料と検索を先に引く経路が起動契機に無く、
  提案の縮小規定が反映先と分量だけでなく是正手段の削減にも働いた。
  対策: 振り返りの提案縮小で是正手段の集合を減らさない規定を追加し、
  ホスト機能の可否判定を`agent-toolkit:agent-standards`の起動契機へ加える改訂をフィードバックへ投入
- 2026年8月27日: session-reviewで、未完了作業への利用者介入を観測したにもかかわらず、メインが長時間待機後に既存規範・重複・過剰設計を先に評価し、「提案無し」で終端した。
  直接的原因: 原因と既存対策の有効性を確定する前に抑止判定を行った。
  混入要因: `75a3efa4`で原因分析の責務をadvisorからメインへ移した際、介入対応と被覆の完了条件を移さなかった。
  動機的要因: advisorを問題列挙に限定し、判断の重複と実行時間を減らすことを優先した。
  見逃し原因: 「提案無し」テストが利用者入力と証拠位置だけを確認し、4原因区分、三層処置、抑止判定の順序と根本原因単位の被覆を検査しなかった。
  根本原因: 判断責務の移管時に実害の是正を抑止判定より先に完了する不変条件を移さず、既存規範の存在を有効性の根拠へ反転できる状態を残した。
  対策: 原因分析・類似見直し・三層処置・根本原因単位の被覆を抑止判定より先に確定し、契約テストで4原因区分、三層処置、部分被覆、全被覆と最終報告の根拠を検査する。advisorの問題列挙専任契約は維持する
- 2026年8月28日: session-reviewのメインがadvisorの確認済み利用者入力と問題locatorを構造検収するため、本文を含む既定の証拠抽出を再実行し、イベント数の多いtranscriptで出力上限による欠落を発生させた。
  直接的原因: 位置情報だけで成立する構造検収に、問題発見用の本文を含む既定出力を共用した。
  混入要因: advisorとメインの責務分離時に、同じ抽出結果を再利用すれば整合すると判断し、消費主体ごとの必要フィールドを分けなかった。
  動機的要因: 新しい抽出機構と状態を増やさず、既存の出力とlocator契約を再利用することを優先した。
  見逃し原因: 契約テストがイベント位置の安定性と被覆訂正を検査する一方、メイン検収の出力フィールドと問題locatorの出力予算を検査しなかった。
  根本原因: 問題発見用の本文と構造検収用の位置情報を同じ照会契約へ結合し、消費主体が必要としない本文を渡さない不変条件を設計・テストへ持たなかった。
  対策: 既定抽出と同じ順序の位置情報だけを返す索引照会へメイン検収を切り替え、問題locatorから既定照会を除外し、出力フィールド・順序・照会回数・既定出力の後方互換性を契約テストで固定する
- 2026年8月: 振り返りの証拠抽出器が、Codexホストの委譲呼び出しをMCPツール名の完全一致で判定し、JavaScript本文を評価してツールを呼び出すexecツールの入力へ埋め込まれた実記録の呼び出しを認識しなかった。委譲先の記録が実在しても走査対象数は0だった。
  直接原因: 抽出条件を対象ホストの実記録ではなくMCPツールの呼び出し仕様から想定した形状で定義し、契約テストの検体も同じ想定形状だった。
  対策: 抽出条件をホストの実記録に現れる格納形状で定め、契約テストの検体を実記録と同じ形状にする
- 2026年8月: 同じ証拠抽出器が、委譲先の識別子へ実行系を必須キーとして与えたため、実行系の指定を伴わない継続・停止の呼び出しで得た識別子を誤った実行系で解決し、解決済みの記録を未確認範囲として重複報告した。
  直接原因: 実体の所在を決める属性を識別子と同格の必須キーとして扱い、当該属性を取得できない入力の存在を設計へ反映しなかった。
  対策: 属性を探索先を選ぶヒントへ降格し、両方の実行系を探索してから未解決と判定する
- 2026年8月: 同じ証拠抽出器で、可変長の値を取るオプションが後続の位置引数を取り込み、対象記録が未指定のまま代替出力を終了コード0で返した。同型の呼び出しが1セッションで16件発生し、証拠を取得できないまま分析が進んだ。
  直接原因: 必須入力の欠落と、記録を読めたが形式を判定できない状態を同じ出力と終了コードで表現した。
  対策: 可変長の値定義を撤去して順序への依存を除き、必須入力の欠落を終了コード2で返す

## セキュリティ規範の回帰

- 2026年7月: codex呼び出しの`sandbox`指定を`danger-full-access`から弱める改変が過去に複数回発生し、
  当該値を弱める改変は事故に当たり、差し戻しの対象とする。同種の弱体化提案はレビュー・改善提案の形でも再出現する。
  直接原因: 当該値を維持する理由が規範本文に無く、安全側への改善として弱体化が提案され続けた。
  対策: 弱めない理由（承認待ちから復帰せず完了検知不能）を規範本文へ明記

## 規範の消失・陳腐化

- 2026年9月1日: 検証コマンド出力の切り詰めに対するフック警告が1セッションで16件発生し、複数の実行主体が同じ操作を反復した。
  直接原因: 初回取得での切り詰めを出力形式によらず禁じ、代替手段を分離実行に限定したため、
  構造化出力から必要な情報だけを取得する低コストの手段が規範上の選択肢に無かった。
  対策: レコード単位で種別を判別できる構造化出力について、種別を指定した抽出を初回取得の観測手段として位置づけ、
  抽出結果を網羅性・件数・終端の根拠にしない条件を併記する
- 2026年8月: 過去の簡潔化コミット群が、先送り表現禁止・不採用根拠限定などの実害ある規範を
  削除しており、数週間気づかれず遡及調査に約30万トークンを要した。
  直接原因: 削減編集で削除の意図（重複解消か設計転換か）を記録・判定する手順が無かった。
  対策: 削減編集での削除理由記録の義務化（agent-toolkit-editスキル「消失検査」節）と消失規範の復元
- 2026年8月: 削減リファクタリング（約350行から約100行）が通常型フィードバックの計画統合規定を削除し、
  同一バッチの項目を1件ずつ別計画へ分ける運用へ反転した（同型の前例あり）。
  直接原因: 削除理由の記録を欠く削減編集。
  対策: 削除理由の記録を義務化し、pickerが通常型の同一バッチを1つ以上の通常レーンへ明示的に分ける。
  各通常レーンは1つの計画と専用worktreeを持ち、変更対象ファイルの重なりと変更規模はレーン割当の入力にしない。
  レーンを分けた後は変更ファイルが重複しても待機しない既存の対策を維持する
- 事象: 2026年8月24日の6レーン並列実装で、先行成果依存の無い4計画を変更対象の交差により1レーンへ割り当て、そのレーンの処理時間が7.82時間となった
- 原因: 変更対象の交差と変更規模をレーン統合の判断入力とし、実装担当の処理時間が加算されるクリティカルパスを考慮しなかった
- 対策: 独立した計画ファイルは計画ファイルごとの専用レーン・専用worktreeへ割り当てる。複数計画の同一worktree共有は、明示的な先行成果依存と統合順を進捗ログへ記録した場合に限る。変更対象の交差は割当時に避けず、rebase時に解消する
- 2026年7月: 「完遂原則」項の削除後も9箇所が実体のない参照を継続し、複数規範の典拠が失われた。
  直接原因: 削除時に参照元を洗い出す工程が無かった。
  対策: 節・規範の削除時に参照元の横断grepを義務化

## レビューの発散・目的変質

- 2026年8月: 利用者合意に基づく撤去計画で、旧IPythonプロファイルの親ディレクトリまで削除対象を広げる一方、
  カテゴリ機構ではCLI以外の利用経路を撤去対象から漏らし、重大指摘2件と利用者への再確認1回を要した。
  直接原因: 合意範囲と実装上のパス・識別子集合を対応付ける工程が計画様式に無かった。
  対策: 撤去・削除対象を実測したパス・識別子集合で記載し、合意範囲との差は計画起草前に確認する規定を追加
- 2026年8月: 同一事実を計画の5箇所へ重複記載した結果、同期漏れでレビューが13ラウンドに達した。
  直接原因: 変動事実の記載箇所を1箇所に限定する様式が無かった。
  対策: 変動事実の名前付きSSOT集約（計画様式の1事実1箇所原則）
- 2026年8月: レビュー指摘を全件採用したため、削減目的の計画が2行減にとどまり、
  保持合意済みの文面まで変更対象へ含めた（利用者の指摘で発覚した）。
  直接原因: 指摘の契約出所・保持対象を記録せず、目的への累積影響を測らずに採用を重ねた。
  対策: 指摘の契約出所・保持対象の記録と累積比較による目的変質検出
- 2026年8月: レビュー途中に追加した新機構の未確定要素が指摘35件中17件を占めた。
  直接原因: 修正で導入した新機構へ起草時の完成条件を適用しないまま再レビューへ送った。
  対策: 新機構追加時は起草時の完成条件を再適用してから再レビューへ送る
- 2026年8月: キューへ保留操作と一覧区分を追加する計画が13ラウンドのレビューを経て、要件に無い永続編集セッション、
  Git trailer、滞留commit復旧API及びrebase失敗状態まで含む実装へ拡大し、既存のフィードバック追加テスト5件を失敗させた。
  直接原因: レビューで提案された修正要素を新しい要求として扱い、元の利用者目的、公開契約、規範的制約又は再現済み重大欠陥へ
  個別に対応付けないまま、後続レビューで新機構の内部整合性だけを高めた。
  対策: 2ラウンド目の修正前にレビュー起因の全要素を元の根拠へ対応付け、3ラウンド目へ進む前に初版からの累積差分を再監査する。
  対応先のない状態、API、識別子、互換経路、復旧経路及び検査は、実装済みでも次ラウンド前に撤去する
- 2026年8月: `plan-impl-executor`が撤回済みのキュー状態変更を要求する修正方針を事前照合なしで書込担当へ送付し、
  8ファイル・207行と4ファイル・48行の2件のcommitを実装後に破棄した。
  直接原因: 通常モードの書込担当の起動前のレーン又は統合後モードの統合worktree作成時に検収した公開契約と
  承認済み変更を合成し、複数計画では指摘へ適用される全条項との両立を求める認可照合が修正後にしか無かった。
  対策: `plan-impl-executor`が対応方針確定前にモード別の作業前OIDを基準とする修正認可の上限へ照合し、
  正常な差異は許容した上で、実際の超過、対応付け不能、計画間衝突を書込担当へ渡さず呼び出し元へ返す
- 2026年7月: 1件の要求に対し計画が肥大化し、25ラウンドのレビューを要した末に当該計画を差し戻した。
  直接原因: 要求範囲に対する成果物規模の妥当性を点検する観点が無かった。
  対策: 要求範囲との規模妥当性点検をレビュー観点へ追加
- 2026年8月: `plan-impl-executor`がレビュー修正の書込担当へ送った起動文の命令が「指定worktree内で最小修正し、検証してcommitせよ」に留まり、受信者が所有する採否判定と類似箇所の一括修正を求めていなかった。
  直接原因: 委譲の共通契約が、起動文の命令と受信者側正本の手順範囲との関係を規定していなかった。
  対策: 命令が受信者の手順範囲を狭めない規定と、受信者が適用する規範スキルを起動文の必須入力へ含める規定を委譲の共通契約へ追加
- 2026年8月: 計画レビューの論点が計画ファイルの記述形式へ移り、レビューと修正が収束せず反復した。
  直接原因: レビュー担当の除外規定が記述スタイルだけを挙げる一方、計画本文の矛盾を一律に指摘対象とし、
  指摘受領側にも記述形式だけの指摘を不採用とする境界が無かった。
  対策: 記述形式を計画レビューと修正の対象外とし、実装時に解消できず実施可能性又は公開契約の成立を損なう矛盾だけを扱う
- 2026年8月: session-review advisorの実装読解を縮小した後も、提案の反映先、代替案、ライフサイクルコスト及び他階層評価に、
  メインが実施するリポジトリ照合を要求する粒度が残った。
  直接原因: 実行手順の責務再配分に対して、出力欄と契約テストの情報粒度を同期していなかった。
  対策: advisorは対象ファイル単位、概念比較、受領範囲の抑止評価だけを返し、節・関数・行・同期対象・実装根拠の確定をメインへ集約する
- 2026年8月: 計画レビュー担当がレビュー表の操作書式と構造検査スクリプトの所在を確定するため、私有実装とファイルシステムを探索した。
  直接原因: 公開CLIの自己記述を操作書式の正本にせず、構造検査スクリプトの絶対パスを初回・再レビューの入力へ渡していなかった。
  対策: `atk review-table --help`とサブコマンドの`--help`を公開正本とし、計画担当・調整主体が解決した`check_plan_file.py`の絶対パスを全経路へ渡す
- 2026年8月: 同じ計画の初回レビュー後も、後続ラウンドで初回から判断できた要件欠落が反復して見つかった。
  直接原因: 初回レビューの走査手順を完了報告の要件別被覆証拠へ接続せず、調整主体が未走査を検出できなかった。
  対策: 独立要件ごとの観点別被覆を完了報告へ要求し、欠落時は完了を受理しない
- 2026年8月、レビュー修正後の報告が`履歴書換え防止`を欠き、同じ報告の再要求を反復した結果、約40分・7ターン停滞した。`completed`はレーン資源回収後に返るため、欠落を検出してから無指定reflogを読む方式では旧OIDを復元できない。第2ラウンドで初回`review_round`のHEADを基準OIDへ保存する案を追加したが、現行`review_round`は第1ラウンドの修正後にしか返らず、最初の履歴書換えを検査できなかった。同じ機構が2ラウンド連続で不成立となった原因は、`review_round`をラウンド完了後だけの返却点とし、履歴書換え前の中間受渡しを一律に禁止した規範前提である。executorは修正担当起動前の`before_fix` checkpointで修正前OIDを公開し、修正後の`after_fix`で修正前後OIDを返す。呼び出し元は`after_fix`の禁止条件検査に合格した後だけ比較基準を更新する。`no_fix`は両OIDの一致だけを検収して比較基準を維持し、不一致時は実測値付き`needs_escalation`へ返す。禁止条件の検査は`after_fix`と最終`merge_request`だけに適用する。実装担当が履歴書換えを開始した後の単一担当・公開済み判定、通常の必須報告契約及び資源回収順序を維持する
  後継方針（2026年8月29日）: 実装担当が修正前後OIDと履歴書換え防止の結果をレビュー表へ保存し、同じ担当が統合前にGit実体へ照合する。会話履歴の欠落時も保存済み完了内容から回復し、準備中の証拠しかない場合は無指定reflogへ後退せず停止する。調整担当の詳細検収と`before_fix`・`after_fix`・`no_fix` checkpointは廃止した
  後継方針（2026年8月29日）: 初回レビューで成果物全体を確認する責務はレビュー担当に残し、調整担当による被覆報告の再検収は廃止した。各ラウンドは指摘件数だけを返し、第3ラウンド以降はメインがレビュー指摘管理表を確認して介入する
- 2026年8月: 実装レビュー中もラウンド別チェックポイントが返らず、反復レビューへの介入が遅れた。
  直接原因: 呼び出し元が受領済みチェックポイントだけを検収し、レビュー表の進行と未着状態を照合していなかった。
  対策: `review_round`を各ラウンドの返却点として維持し、第3ラウンド以降はメインが反復原因と計画目的への累積影響を検査する。進行中の未着と無進行は一般の待機・停滞監視で扱い、レビュー表の行数を稼働又は完了の根拠にしない
  同じ時期、計画レビューで確定した初回被覆の確認を実装レビューへ横展開しなかった。
  通常実装・差分限定・統合差分・CI修正後の各経路で、計画照合と成果物評価の担当、起動数及び`track`が一致しない事象が発生した。
  直接原因: 共通の実装レビュー契約を呼び出し元と受領側へ同期せず、経路ごとに計画照合と成果物評価を別の担当と別の`track`へ分散させた。
  さらに、再レビューで前回レビュー後の修正と直接影響範囲を限定せず累積差分全体を対象にしたため、未変更部分で初回から判断できた指摘が後発して登録された。
  当時の対策: 実装レビュー担当を`implementation-review`の単一trackへ統合し、計画照合後の成果物評価、初回被覆結果とラウンド合計を同じ完了契約へ揃えた。
  再レビューは前回レビュー対象OIDからの修正差分、採用・未解決指摘、直接影響範囲と新設・変更契約へ限定した。
  共有parser、共通状態、公開契約、生成元又は配布元を変更した場合だけ実際の全消費先へ拡大した
  後継方針（2026年8月29日）: 初回は全体をレビューし、再レビューはレビュー指摘管理表、修正箇所及び直接影響範囲だけを確認する。調整担当の詳細報告と再検収は廃止し、作業担当が修正、検証及び統合の完了責任を持つ
- 2026年8月28日: 通常型フィードバックの計画型変換で、レビュー収束後の最古項目を実装開始前に`processing`へ移し、その遷移をユーザー指示として記録した。
  直接原因: 技術的に不成立な一時`processing`案を専用`planning`へ変更した際、元案へ依存した成功後の状態と処理主体を再導出せず、各状態遷移の要件出所を検査しなかった。
  対策: 変換結果を`inbox`へ置いて処理開始を`start-processing`又は`process-loop`だけに戻し、代替設計時は依存判断を未確定へ戻して各状態遷移の主体と出所を計画・レビューで照合する
- 2026年8月28日: 計画構造検査と後続の検索・レビュー表検証を同じシェル呼び出しへ連結したため、構造エラーを返した終了コードが後続コマンドで上書きされ、計画レビューへ進んだ。
  直接的原因: 計画構造検査を独立したコマンドとして実行せず、シェル呼び出し全体の最終終了コードを合否に用いた。
  混入要因: 終了コード・警告・修正概要を保持する契約はあったが、構造検査の実行単位と終了コード0だけの後続ゲートを定めていなかった。
  動機的要因: 複数の確認を1つのシェル呼び出しへ集約した。
  見逃し原因: 契約テストが構造検査スクリプトのパスと終了コード文言だけを確認し、独立実行を確認しなかった。
  根本原因: 合否検査の実行単位と後続工程の開始条件を、計画担当・レビュー担当の共通不変条件にしていなかった。
  対策: 両担当が計画構造検査を独立したコマンドとして実行し、同検査が直接返した終了コード0の場合だけ後続工程へ進む契約と、その同期テストを追加する
- 2026年8月28日: ユーザーが明示した対象を計画が狭めたにもかかわらず、対応する確認済み回答がないまま計画レビューを完了した。
  直接的原因: 明示要件との差を技術判断として`## エージェント判断`へ記録し、確認済み回答の代替として扱った。
  混入要因: 作成基準と共有規範には確認義務があったが、起草手順とレビュー完了ゲートへ同じ条件を接続していなかった。
  動機的要因: 調査結果に基づく対象の具体化は自律確定できると解釈した。
  見逃し原因: レビュー契約は事後承認扱いを禁じたが、確認済み回答がなければレビューを完了しない条件を明記していなかった。
  根本原因: 明示要件との差、確認証拠、起草・レビューの終端を1つの検出条件として定めていなかった。
  対策: エージェント判断の提示は確認済み回答の代替にならず、明示された対象・範囲・外部可視結果との差に回答がなければ起草とレビューを完了しない契約と、その同期テストを追加する
- 2026年8月28日: 同じ契約の定義・直接消費側・契約テストを話題別の三実装単位へ分けたため、先頭単位の適用後に204件中43件のテストが失敗した。
  直接的原因: 中間commitの近接検証に必要な対象を別の実装単位へ分割した。
  混入要因: 実装単位の独立性を成果・commit・対象ファイル集合・近接検証で表したが、変更定義から直接消費側・契約テスト・生成配布物までの必要範囲を定めていなかった。
  動機的要因: 中間commitの成立より話題別の小さなcommitを優先した。
  見逃し原因: 契約テストが抽象的な独立性文言だけを固定し、直接依存の分割禁止を確認しなかった。
  根本原因: 実装単位の境界を、中間commitの正式な近接検証が成立する直接依存範囲として定義していなかった。
  対策: 変更定義・直接消費側・契約テスト・生成配布物のうち中間commitの正式な近接検証に必要な対象を同じ単位へ含め、先行commit単独で近接検証が成功する場合だけ分割する契約と、その同期テストを追加する
- 2026年8月: 実装後のレビュー指摘対応で、承認済み計画の前提・方針・意図に反する修正が反復して確定した。
  ユーザー指示に由来して固定集合へ変更した内容を、既存の規範に反するとして実装工程の判断だけで取り消した事例を含む。
  直接的原因: レビューイー側契約が、過去の合意・保持対象の維持だけを規定し、承認済み計画の確定内容を覆す修正の禁止と矛盾観測時の差し戻し経路を持たなかった。
  混入要因: 実装担当の入力契約が計画の実施内容と変更履歴を採否・範囲の上限としてのみ規定し、範囲の拡大だけを禁じて確定内容の反転を禁じなかった。
  動機的要因: 現行実装と既存規範を最新の判断材料とみなし、計画側の確定内容の由来を調べずに下位の作業メモとして扱った。
  見逃し原因: 実装レビューの再レビューが修正箇所と直接影響範囲だけを走査し、計画の確定内容へ照合する条件を持たなかった。
  対策: 実装工程では計画の確定内容を覆す修正を確定せず、双方の由来を調べて優先順位を適用し、計画側が上位又は由来を確定できない場合は呼び出し元へ返して確認経路で解消する。再レビューでも修正箇所を計画の確定内容へ照合する
- 2026年8月: 利用者専用のmigration操作であるとの是正後も、計画レビューが別操作のagent間排他をmigrationへ拡張し、レビュー起因のlease復旧条件を利用者要件として扱った。migration用lock・leaseの追加と撤去が反復する一方、新保存先への書込認可という正常系の欠落は第5ラウンドまで検出されなかった。直接原因は要件出所と適用条件を操作単位で保持せず、主要な異常系を通常経路より先に検討したことである。対策は、正常系の起動、書込み許可、配布及び成果到達を先に端から端まで走査し、状態管理の指摘を要件出所、対象操作、操作主体、競合主体及び同時成立条件へ対応付け、前提失効時に依存する指摘と追加機構を次回レビュー前に撤去することである
- 2026年8月: レビュー指摘への対応でコーディングエージェント向け文書の変更後の確定文面を全面差替えした際、読込経路と証拠抽出器の絶対パス受け渡しが欠落し、後続2ラウンドで検出された。直接原因は、運用契約を移す全面差替えを接続再列挙から除外する単純な文面変更として扱えたことである。対策は、運用手順、参照資料又は直接操作を含む全面差替えへ既存の接続再列挙を適用し、差替え前後の読込経路、対象・入力・取得経路、絶対パス及び契約テストを照合することである

## フック・セッション状態の不全

- 2026年9月1日: フック定義が参照するスクリプトを解決できず、PreToolUseのツール呼び出しが17件遮断された。
  直接原因: コマンド定義が終了コード2を条件なく遮断へ変換し、判定による遮断とスクリプト不在によるuvの終了を区別しなかった。
  対策: 個人環境向け設定のPreToolUseへ参照先の実在確認を前置し、不在時は判定せず終了コード0で終える。
  agent-toolkitプラグインのフック定義へ同じ確認を導入しないのは、定義と実体が同一のplugin root配下で同時に配布され、
  POSIXシェルによるラッパーがWindows実行環境で成立しないためである
- 2026年6月: Windowsでフックのパス判定が常に偽となり、編集が恒常ブロックされた
  （利用者はセッション状態の直接書き換えで回避した）。
  直接原因: バックスラッシュ区切りパスの未正規化。対策: フック2スクリプトのパス区切り正規化
- 2026年8月: 個人配布フックが8種類の編集保護を無効化したまま動作していた。
  直接原因: `ModuleNotFoundError`発生時も終了コード0を返す実装で、隔離実行の検証も無かった。
  対策: 依存分離と隔離実行のsmoke test
- 2026年8月: 状態フラグがセッション途中で失われ誤警告が13回反復した。
  直接原因: 状態ファイルの回収がセッション終了理由に依存し、稼働中でも削除された。
  対策: 状態ファイルの回収を期限方式へ変更
- 2026年7〜8月: 状態フラグが立たず正当な工程が誤ブロックされる事象が複数セッションで再発した。
  直接原因: 完了報告の書式・配送経路の揺れを、フラグ記録側の抽出仕様が許容していなかった。
  対策: 抽出仕様の許容拡大・SubagentStop経由の真化経路追加・ブロック文言の是正
- 2026年8月: PreToolUseのBash検査が、検証ツール名・禁止コマンド名を引数として含むだけの
  読み取り操作へ誤発火した（切り詰め警告を実セッションで3回観測）。
  直接原因: 検出条件をコマンド文字列全体への部分一致で実装し、対象が実行位置にあるかを判定していなかった。
  対策: 実行位置のトークン列を返す共通ヘルパーへ助言用3検査を集約し、引数位置の負例テストを追加。
  遮断検査はコマンド置換・オプション終端まで解決する解析を用意できるまで現行判定を維持する
- 2026年8月: フック警告が案内する対処自体が同一原因で再ブロックされる構造や、
  規範間で両立しない警告（検収補正時のやり直し誘導）が観測された。
  直接原因: 警告文の対処案内が実際の原因・正当な継続経路と乖離していた。
  対策: 警告文へ実際の原因と継続経路を明記
- 2026年8月: 単発のAgent・Task又はagents_server起動が、`agent-toolkit:delegation`の起動記録が無いことだけを理由にPreToolUseで遮断され、対応するスキル起動時にも警告が出ていた。
  直接原因: 単発の基本委譲と、経路選択・継続・複数主体調整を要する高度な委譲を、フックの状態フラグで同一の必須経路として扱っていた。
  対策: 基本委譲の対象・権限・完了条件・受領後の実測検収を常設規範へ置き、高度な経路だけへ委譲スキルを適用する。PreToolUseのゲート・警告及びPostToolUseの専用状態記録を撤去し、agents_serverのcwd・継続入力など独立した入力検査は保持する
- 2026年8月: `SubagentStop`が未消化の子エージェント起動を理由にblockを返し、待機中の調整役のターン終了を拒否した。
  調整役にはその場で状態を変える操作がなく、無操作のコマンド呼び出しが反復し、利用者が2回中断した。
  直接原因: 対象主体の同一ターン内の行動で解消できない他主体の状態を、`SubagentStop`の終了条件にした。
  対策: `SubagentStop`から未消化の子エージェント起動の検査を撤去し、対象主体が同一ターンで解消できるblockだけを残す。
  block通知は解消手段を伴う単一の整形関数へ集約する
- 2026年8月: 直接の子エージェントが完了済みでも、子のtranscriptで起動した孫エージェントが稼働中のまま、
  最上位Stopが通常終了を許可した。
  直接原因: 最上位transcriptだけを走査し、直接の子記録にある孫起動と、最上位の`queue-operation`にある完了通知を同じ集合へ統合していなかった。
  対策: 共有Stop判定器が固定された子記録から孫起動とtask-id対応を収集し、最上位生transcriptの`enqueue`・`remove`完了通知で相殺する。
  LLM実行主体への完了通知配送契約とは観測面を分離し、`SubagentStop`の子孫blockは復活させない
- 2026年8月: 直接子がいったんCompletedを返した後、孫の完了通知又はSendMessageで同じagent IDのまま自動再開しても、親transcriptには新しい起動イベントが無く、Stopが未完了0件としてセッション振り返りを誘導した。
  対策: Stop入力の`background_tasks`にある非`teammate` taskを現在状態の根拠として共有判定へ接続し、transcript推論は当該申告を得られない旧ホストとCodexの代替入力とする。
  `SubagentStop`の子孫blockは循環待ちを再発させるため復元しない
- 2026年8月28日: 複数レーンの計画レビュー中に、先に完了した計画担当へ指摘を返す時点で30分の結果保持期限を超え、`send_message`が`session retention expired`を返して同一会話を再開できなかった。
  直接原因: Codexの`thread/resume`とClaude Agent SDKの`resume`は保持中sessionの内部replyだけから利用でき、期限切れ後に呼び出し元の正確な識別子から到達する公開入力がなかった。
  背景原因: 初期移行時に結果本体とClaude接続の有限時間回収だけを検証し、長時間オーケストレーションが保持期限後も同じ担当へ指摘を返す利用シナリオを契約テストへ含めなかった。
  対策: 結果本文の回収後も`engine`・`cwd`・`model`・`effort`を最小状態として保持し、同じ`send_message`が両engineの保存済み会話を暗黙に再開する。両engineの期限切れ再開と正確な識別子配送を近接テストで固定する

- 2026年8月30日: `engine=claude`の委譲先への`send_message`が応答を返さず、その後の`wait`が`session retention expired`を返して委譲工程が停止した。
  直接原因: 継続要求の応答futureを解決する主体がsession所有タスクの正常経路だけであり、保持期限の到達、message streamの例外、タスクの取り消しで所有タスクが終了する経路に未解決futureを解決する処理が無かった。
  背景原因: 同一実装のCodex側は接続終了時に未解決の要求を解決していたが、Claude側の継続要求キューは所有タスクが生存している間だけ要求が届く前提で設計され、契約テストも所有タスクの生存中と終了後の定常状態だけを固定していた。
  対策: 要求の受理と応答の解決を1つの継続要求チャネルへ集約し、所有タスクのいずれの終了経路でも受理済みの要求を解決する。所有タスクが終了したsessionへの継続送信は保持期限の経過と同じ再開経路へ回し、保持中の直前結果を`previous_result`として返す

- 2026年8月: `agents_server` MCPの初回engine選択が、MCP起動後に置換されたプラグイン配置のローカルbackendを解決できず失敗した。
  直接原因: backend class importをengine選択時まで遅延し、MCP初期化時にローカルmoduleの存在を確定していなかった。
  対策: 共有状態moduleで循環importを解消し、Codex・Claude backend classをMCP初期化時に読み込む回帰検査を追加する
- 2026年8月: `agents_server`のPEP 723環境ウォームアップが成功しても、Claude Agent SDKのimport不足をClaude turn開始まで検出できなかった。
  直接原因: `--help`だけを実行し、lazyなSDK importとClaudeAgentOptions構築へ到達していなかった。
  対策: `--check-dependencies`でoptions構築まで検査し、Claude CodeとCodexのagents_serverウォームアップへ適用する
- 2026年8月28日: Codexホスト経由の`agents_server`起動が`Codex App Server stdout closed`だけを返し、
  子プロセスの終了コードと標準エラーを呼出元から確認できなかった。
  直接原因: 標準エラーをMCPプロセスの標準エラーへ転送するだけで保持せず、標準出力終了時の例外を固定文にしていた。
  対策: 子プロセスの終了コードと標準エラー末尾を有界に保持し、標準出力終了時の例外とMCPログへ含める

- 2026年8月30日: フック定義が参照する共通入口スクリプトの改名が作業ツリーへ統合された時点で、
  稼働中のメイン1セッションと委譲先6セッションの全ツール呼び出しが拒否された。
  直接原因: hook定義はセッション起動時に読み込まれ、`command`のスクリプトを起動時点のパスで参照し続けるため、
  旧パスの実体が消えるとフック実行が起動前に失敗した。
  対策: フック定義が参照するスクリプトの改名・移動・削除では旧パスへ委譲専用の互換入口を残す。
  撤去は新しい入口を含む版の配布後の版数更新以降であり、かつ旧定義を読み込んだセッションの終了を確認できた場合だけとする
- 2026年8月30日: 常駐実行の委譲先セッションで自律終了の再促が毎ターン発火し、委譲範囲外の終了工程を求められた。
  直接原因: 発火条件を常駐実行の印である環境変数だけで表し、当該変数が委譲先セッションへ継承された。
  対策: 委譲先セッションの印をセッション起動経路が付与し、発火条件を最上位セッションへ限定する
- 2026年8月31日: 常駐実行の最上位セッションが`agent-toolkit:exit-session`を起動しないまま終了した。
  直接原因: Stop入力の`background_tasks`が空の一覧を渡す一方、transcript復元による未完了の起動記録が残り、
  共有Stop判定器が両者の論理和で継続中と判定して終了を承認した。
  対策: 有効な`background_tasks`一覧を正本とし、transcript復元の未完了残差を継続根拠へ加えない。
  常駐実行の起動目的文へ`agent-toolkit:exit-session`によるセッション終了を含める
- 2026年8月31日: 委譲先セッションの自律終了再促を最上位セッションへ限定する修正を確定した後も同じ遮断が観測され、
  既存の除外判定が実効的でないという診断が記録された。その後の実測では、委譲サーバー経由で起動した委譲先の
  停止判定が委譲先として承認され、除外判定は現行経路で成立した。
  観測できた事実: 遮断の記録は修正commitの時刻より後にもあるが、当時の委譲サーバープロセスの
  起動スクリプトのパスと起動時刻は残っておらず、当該プロセスが読み込んだ版を確定できない。
  常駐プロセスが修正前のモジュールを保持していたことは、観測された時間対応を説明できるが断定はできない。
  対策: `agent-toolkit/scripts/`配下の修正後に同じ事象を観測した場合は、稼働プロセスの起動スクリプトのパスと
  起動時刻、及び当該パス配下の対象ファイルの更新時刻を比較して読み込んだ版を確定してから、修正の有効性を判定する。
  過去のプロセスの証拠を回収できない事象は、版差を直接原因として記録せず、現行経路での再現可否を結論とする

## 停滞・空転

- 2026年8月: 担当外だった`mise.lock`の変化が別セッションで解消された際と、別セッションの無関係なcommitで
  基準OIDが変化した際に、担当対象へ影響が無いにもかかわらず契約違反の可能性として監査し、
  ユーザー確認や再計画へ進んで工程を止めた。
  直接原因: 記録時点を示すOIDと担当外差分の不一致を、実質影響と区別せず停止条件として扱う規範しか無かった。
  対策: 参照情報としてのOIDと担当外差分の変化を停止条件から外し、担当対象・公開契約・書込所有権への
  実質影響で続行可否を判定する横断規範を置く。操作対象を完全OIDで指定する既存契約は維持する
- 2026年8月: Codexの名前付き役割適用、実際の別主体への委譲と外側の待機を別のライフサイクルとして扱わず、差分・HEAD・無応答・経過時間を重ねて`running`中の委譲を停滞判定する事象が発生した。直接原因は役割適用と実主体の境界、補助観測と終端statusの優先順位が待機契約へ分離されていなかったことである。対策は、名前付き定義の直接適用を外側委譲へ変換せず、Codexの`running`中は補助観測だけで中断しない契約をbaseと待機文書へ置くこと、加えて広範な未完了調査を独立単位へ分けることである
- 2026年8月: 待機報告へ並行主体を含む作業ツリー全体の差分件数・HEAD又は別主体のartifact値を帰属なしに載せ、委譲先の進捗と解釈し得る事象が発生した。対策は、書込対象と書込主体を報告へ結び付け、`atk watch`の全体値を単独の生存根拠にしないことである
- 2026年8月24日: 孫の完了報告が呼び出し元へ配送されず、呼び出し元が成果物とtranscriptの最終更新時刻を実測して終了を検出した。
  同時点でも`ListAgents`は孫を`running`と表示し、定時起動を装着していなかったため復旧の契機が無かった。
  直接原因: 完了通知の配送不着時に、独立した定時起動から停滞の検知と巻き取りへ進む経路が無かった。
  混入要因: `claude-code-runtime.md`が`ScheduleWakeup`と`CronCreate`を一般待機の候補として並記し、Cronの作成・再利用・resume又はcompaction後の照合・削除を一連の契約として定義していなかった。
  動機的要因: 完了通知を主経路とする待機設計に、通知不着時の復旧契機を定期再確認で保持する要求が組み込まれていなかった。
  見逃し原因: 主要agent定義と`_REQUIRED_TOOLS`に`CronCreate`、`CronList`及び`CronDelete`がなく、`_stop_gate.py`と実際のCron待機を結ぶ検査も不足していた。
  根本原因: 完了通知、ホストの定期起動、Stopの継続判定とagent定義のtool許可を別々に管理し、待機対象と定期taskを同じ正本状態へ結び付けていなかった。
  是正: `atk wait-schedule`のcron式を変更せずに1件だけ`CronCreate`へ渡し、保持IDとtask IDを`CronList`で照合して再利用し、全対象の終端後に`CronDelete`で削除する契約をruntimeと待機文書へ置く。定期再確認時は更新時刻だけで終了を判定せず、記録末尾と実行識別子を照合して停滞の検知と巻き取りへ進む。
  横展開: `CronCreate`をStopの非同期待機系と3つのagent定義の許可toolへ追加し、完了通知を既定経路として維持する実機相当の検査を追加する。
  再発防止: `ScheduleWakeup`は`/loop`専用として一般待機から除外し、シェルの`sleep`や背景タイマーを追加せず、Cronの作成・再利用・照合・削除を1つの意味検査で検出する
- 2026年8月: 孫へ委譲した調査が完了しても、直接の委譲元へ成果が配送されず、祖先が受領した完了通知の中継も成立しない事例で同じ調査を再実行した。
  直接原因: 孫調査の起動文に成果ファイルの絶対パスと記載形式がなく、既存の記録ファイル直接読取は孫がファイルを書き込む契約を持たなかった。
  対策: 孫調査・レビューの起動前に管理対象一時領域内の成果ファイル絶対パスを確定し、専用タスク文書又はagent定義の完全な完了報告を同期的に確定してから受信者が終端する。直接の委譲元は完了通知の到着を成果本文の受領条件にせず、実行時に観測できる受信者の終端後にファイルを再読する。読み取り専用受信者へ所有プロセス終了証拠を要求せず、終端未観測・欠損・読取不能・不完全な成果は成功扱いしない。`needs_escalation`として一時領域を保持する
- 2026年8月: 10分周期の定時起動を装着したセッションで、1時間ごとの進捗報告が3時間以上発生せず、利用者の介入が2回発生した。
  直接原因: 定期promptが待機対象の正本状態だけを再確認し、経過時間で発火する定期報告の義務、実測コマンド及び判定閾値を含まなかった。
  対策: 定時起動の装着時に、当該セッションへ適用される定期報告、cooldown解除、期限監視などの経過時間起動義務を列挙し、実測コマンドと判定閾値をpromptへ接続する。該当義務が無い場合は追加しない。固定周期の短縮と新しいschedulerは行わない
- 2026年8月: 遠隔ホストのGPU互換性検証を`nohup`で切り離して起動したため、前景のSSH呼び出しが即座に終了し、遠隔ジョブが終了コード1で完了しても完了通知が届かなかった。完了まで同じログを5回照会し、1回は前景の`sleep`を伴った。
  直接原因: 実行主体が応答可能な検証へ、実行主体自身が応答不能になる切替工程でだけ必要だった遠隔切り離しの起動形を流用した。
  対策: 遠隔側の`nohup`又は`setsid`による切り離しは実行主体自身が応答不能になる工程だけに限定し、それ以外の長時間コマンドは`run_in_background=true`でSSH接続を保持するか、対象を限定した前景実行とする。固定周期の短縮は通知経路を回復せず、コマンド形の自動検査は正当な切り離しも同じ形を取るため追加しない
- 2026年8月: セッション識別子の変更後に委譲先の再開を試み、`SendMessage`が`No transcript found for agent ID`で失敗した。
  直接原因: 旧セッション識別子配下の委譲先を再開可能と扱い、配送不能を新規起動へ切り替える条件に含めていなかった。
  対策: 当該エラーを配送不能の実測として扱い、再開を反復せず検収済み状態を渡して新規起動する
- 2026年8月: 自律実行中の確認発行で回答受領まで105分停止した（セッション最大の停止要因）。
  直接原因: 確認への回答期限が無く、発行した確認が回答受領まで工程を止めた。
  対策: 質問の自動継続タイムアウトを配布し、期限内に回答が無い確認をTBDへ退避して続行する。
  期限を提供しない実行環境では応答を得られないと判断した時点を契機とする
- 2026年8月: 自律実行中に計画立案モードへ入り、承認待ちで工程が停止した。
  直接原因: 同モードの終了時承認が自律実行では応答を得られない。
  対策: 自律実行では計画ファイルを直接作成する（厳守規定化）
- 2026年7〜8月: CI監視の待機ハングが複数回再発した。
  直接原因: ワークフローフィルタ不一致・登録遅延・短縮SHA不受理を考慮しない自作待機ループ。
  対策: 専用スクリプト（`wait_ci.py`）の使用既定化と識別子の完全形式使用
- 2026年8月: process-loopのプロセスが80個・RSS約2.33GBまで積み上がった。
  直接原因: 自己再起動の`os.execv`が`uv run`経由の親プロセスを置き換えられず1段ずつ増えた。
  対策: 再起動方式の構造修正
- 2026年8月: process-loopの子セッション正常終了後に、開始前更新と同じ`update-dotfiles --force`が同一反復で二重実行された。
  直接原因: 開始前更新を残したまま正常終了後の配布反映経路を追加していた。
  対策: 反復内は開始前更新1回に限定し、正常終了後の2回目を削除する
- 2026年8月28日: process-loopのCodex起動文が開始後のready項目も同じセッションで処理するよう明示していた一方、
  `agent-toolkit:process-feedbacks`が起動時の対象集合を固定し、追加分を次回セッションへ送る逆の契約を持っていた。
  直接原因: `agent-toolkit:process-feedbacks`の処理集合変更時に呼出元のprocess-loop起動文と既存設計の連続処理条件を照合せず、
  単一処理回の集合固定をセッション全体の固定へ拡張した。
  対策: 各処理回の集合は固定したまま、明示的な連続処理では全レーンの後始末後にready一覧を再取得し、
  0件を確認した場合だけ既存の終了工程へ進む
- 2026年8月28日: `agent-toolkit:process-feedbacks`のpickerが`hold`項目を優先度の根拠に使い、ready項目の選定範囲を狭めた。
  直接原因: `hold`導入時に確定した「activeは表示用、processableだけが自動処理対象」という不変条件を、
  後発のpicker契約が参照せず、pickerへ渡す対象を`hold`を含むactive一覧として定義していた。
  対策: 各処理回はprocessable一覧のうちready項目だけを固定し、既存のplanning、editing、hold及びblocked項目を
  候補、優先度、依存判断及び固有指示の入力から除外する
- 2026年8月29日: `agent-toolkit:process-feedbacks`が5件を選定して計画作成へ着手した後も、選定済みの全件が`inbox`に残り、
  キュー上の`processing`が0件だった。
  直接原因: `pick-feedbacks.md`が対象集合の固定と出力検収までしか定めず、その直後の状態遷移を①の完了条件へ接続していなかった。
  対策: picker出力の検収直後に、選定時点で`inbox`だった集合へ既存の一括`start-processing`を実行し、
  全件の`processing`配置を確認してから計画・worktree作成を含む②へ進む
- 2026年8月29日: 起動時に固定したready集合だけを処理するユーザー指示に対し、全レーン後のready再取得を現行契約として優先した。
  直接原因: 起動時の対象集合をユーザーが確定した外部可視要件として保持せず、後続のエージェント判断で変更できる内部実装詳細として扱った。
  対策: 2026年8月28日の対策として導入した全レーン後のready再取得を撤去し、起動時の固定集合の完了後は
  再取得せず終了工程へ進む契約へ、入口、選定、Codex起動文及び設計をそろえる
- 2026年8月: ログインシェル起動が約13秒かかっていた。
  直接原因: 起動時処理がキューの終端2300件超を毎回全件パースしていた。
  対策: 参照範囲の限定で約0.66秒へ短縮

## 並行実行の競合

- 2026年9月1日: 複数レーンがprivate-notesへ実装前の計画ファイルとレビュー表を書き込み、管理リポジトリが常時dirtyになった。
  `atk mq convert-to-plan`のclean要件を満たせず、remote同期のmergeも未コミット差分と衝突して2回失敗した。
  直接原因: 計画の可変な作業状態と永続保存状態を同じprivate-notesのrootへ置き、計画レビュー時にも保存commitを要求していた。
  対策: 実装レビュー完了までは`~/.claude/plans/yyyy/MM/`で更新し、実装レビュー収束後だけ`atk plans commit`で同stemをprivate-notesへ移動して保存する。失敗時は作業側を保持し、日付階層の作業バンドルを旧形式移行から除外する
- 2026年9月1日: `atk mq process-loop`が管理リポジトリの履歴分岐を解消できず、子セッションを起動しない待機を繰り返した。
  直接原因: pullにはfast-forwardと冗長なMQ終端commitの除外しか回復手段がなく、同等でない分岐を
  常駐ループが反復するたびに同じ失敗へ戻っていた。rebase中間状態の例外も常駐ループの捕捉対象外だった。
  対策: 作業ツリーとindexがcleanな分岐は`rebase @{u}`でローカルcommitを保持して回復し、dirtyな分岐は
  従来どおり手動回復へ委ねる。rebase中間状態は常駐ループが捕捉し、復旧手順を表示して待機を継続する
- 2026年8月31日: 7レーン並行処理中に別cloneが同じMQ項目を近接して採用し、管理リポジトリが分岐して
  `atk mq list --pull`を含む後続操作がfast-forward失敗で停止した。
  直接原因: clone単位のロックでは別cloneを直列化できず、終端commit後のpush失敗でローカルcommitが残る一方、
  処理日時だけが異なる同等終端をGit同期層が一般の競合と区別できず、ff-only pullに回復経路が無かった。
  対策: 分岐時は双方のcommit件数とrebase・skip・abort・pushの手順を表示する。
  作業コピーがcleanで、ローカル側の全commitが管理CLI形式の終端移動だけを含み、upstreamの同名終端と
  処理日時以外の内容が一致する場合に限り、冗長なローカルcommitを除外してupstreamへ自動同期する
- 2026年7月: 並列セッションが同一ブランチへamendを発行しHEAD分岐が複数回発生した。
  直接原因: 並列セッション間でHEADの所有を確認・調停する手順が無かった。
  対策: 分岐解消プロトコルと着手前のgit log確認
- 2026年7月: 同一計画へ実行主体が2つ並走し同一作業ツリーで同時編集が発生した。
  直接原因: 後続の起動条件が先行系の完了報告ではなくHEAD進捗の観測に依存していた。
  対策: 1作業ツリー1書込主体の原則と、先行系の完了報告受領を起動条件へ厳密化
- 2026年8月: 作業用複製が分岐元から21コミット遅れたまま調査・起草し、rebase後に前提が広範に無効化された。
  直接原因: 着手前に分岐元との差分を確認する手順が無かった。
  対策: 着手前の分岐元差分確認を規定
- 2026年8月: 異なる計画ファイルが同じファイルを変更する場面で、別worktreeの並列実装へ進まず先行実装の完了を待機した。
  直接原因: 同じworktreeの書込排他と、異なる計画間の変更内容の競合を同じ待機条件として扱った。
  対策: 書込担当の終端待ちは同じworktreeへの後続起動だけに限定し、異なるレーンは変更ファイルが重複しても別worktreeで並列実装する。
  対象ファイルの重なりと変更規模はレーン割当の入力にせず、異なるレーン間の競合はrebase時に解消する。
  統合時は関係する全計画を全文読み、目的と設計判断を照合して競合を解消する
