# 設計意図

## フィードバック処理の工程別モデル委譲構造

フィードバック処理は、調査と分解を`feedbacks-planner`、実装単位の調整を`plan-impl-executor`、変更の作成をwriterへ分ける。
各主体は担当工程だけの知識を持ち、統合writerがcommitをcherry-pickして履歴を一本化する。
1つの計画ファイルに属する実装単位は1 writerが1 worktreeで順次実装し、異なる計画ファイルのlaneだけを別worktreeで並列化する。
この分担により、計画単位の起動・統合費用を抑えつつ、公開と採用の判断をメインへ集約する。
単一主体へ全工程を集約する案は、並列実装と公開判断の責務境界を失うため採用しない。

## 終端工程

commit以降のリリース、PR/MR又は公開操作は、実装laneと分離して全laneの統合、push及びCI通過の後にメインが実行する。
本文の明示記載を不可逆操作の認可とし、明記のない操作はTBDへ送る。
この順序により実装は並列化でき、公開操作は1回だけ実行できる。
laneへ公開を委譲する案は、公開の重複と認可の分散を生むため採用しない。
