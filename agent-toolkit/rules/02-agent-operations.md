# 02-agent-operations.md: エージェントの実行運用

ツールとコマンドの実行、委譲時の保護、プロセスの終了、識別子の命名について、
製品横断で常に成立させる規範を扱う。

## ツール・コマンド運用

- 独立した複数のツール呼び出し（複数ファイルの`Read`等）は同一応答内で並列に呼ぶ。依存関係がある場合のみ逐次実行する
- 複数の文書を全文読み込む場合は、先に行数またはバイト数を確認し、長文を1つのツール出力へ連結しない。
  切り詰めを観測した場合は取得済みの範囲を再取得せず、未取得の範囲だけを分けて読む（努力目標）
- plugin資源のrootが失効したことを観測した場合、以後は保持済みのrootを再確認なしで再利用せず、
  実行環境が提供するplugin導入情報から現行の導入版とrootを再解決し、
  利用する資源の実在を確認してから後続処理へ渡す
- シェルメタ文字（バッククォート・ドル記号・二重引用符・改行など）を含む本文を単一引数として
  外部コマンドへ渡す場合、本文は`$'...'`のANSI-Cクォートで囲むか、変数へ代入してから`"$VAR"`で展開する。
  プログラミング言語の文字列シリアライザーの出力はシェルの引用規則とは別体系のため、コマンド文字列へ連結しない
- 本文を登録・送信するコマンドが警告またはエラーを出力した場合、終了コードが0でも
  登録結果を取得して本文と照合する。欠落を検出した場合は同じ経路で修復する
- 定型作業の前に、`agent-toolkit/scripts/`配下、各スキルの`scripts/`配下、関連スキル、
  対象リポジトリのタスクランナー定義（`Makefile`・`mise.toml`のtasks・`package.json`のscripts等）を
  確認する。対応する専用スクリプト・スキル・サブコマンド・タスクが存在する場合はそれを使う（努力目標）
  - 複数ファイルに跨る置換は`agent-toolkit:pyfltr-usage`のgrep/replaceサブコマンドを使う。
    長出力のコマンド列は`agent-toolkit:shell-exec`へ委譲する。
    CI通過確認は`agent-toolkit:commit`スキルの専用ツールを使う
  - `AGENT_TOOLKIT_PRIVATE_NOTES`配下の`inbox/`または`processing/`にあるキュー管理ファイルを
    専用サブコマンド以外で変更した場合は、作業を終える前に`atk mq commit`を実行する
- 同一セッションで取得済みのファイルは、変更を観測するまで保持済み内容を再利用する。
  編集後の検収、外部更新後、正確な現行文面が必要な差分適用前に再取得する
- 作業用の複製（git worktree等）内で起動したセッションは、調査・計画作成へ着手する前に
  `git fetch`後の分岐元との差分を双方向で確認し、分岐元が進んでいる場合は先に追随させてから着手する（努力目標）
- 別セッションが同一リポジトリへコミットしうる前提で作業する。
  編集の直前に対象ファイルを再取得し、コミットの前に基準コミットを再確認する（努力目標）。
  自セッションの担当範囲だけをステージし、担当外の変更を混入させない（厳守規定。
  他セッションの未完成の変更を巻き込むとコミットが技術的に成立しない）
- 1つのセッションで複数のリポジトリを扱う場合、`git`コマンドは`-C <絶対パス>`で対象リポジトリを明示し、
  同一のコマンド列へ`cd`と`git`を並置しない（努力目標）
- `git merge`進行中は`git stash`を使わない。退避は別パスへの`cp`または別ブランチ退避で行う
- 外部コマンドの引数となる識別子（ツール名・コミット識別子・タグ名・実行ID・PR番号など）は、
  実行結果として得た値の文字列をそのまま呼び出しへ用いる。短縮形からの復元・記憶・推測で組み立てない

## 委譲時の厳守事項

委譲の送信、継続、受領、停滞判定の手順は`agent-toolkit:delegation`を正本とし、委譲する場面で起動する。
本節には、委譲の有無にかかわらず作業ツリーと規範文書を保護する厳守規定と、
`agent-toolkit:delegation`を起動しない委譲先にも適用される受信側の厳守規定だけを置く。

- 1つの作業ツリーへ書き込む主体は同時に1つだけとする。
  新しい書込担当を起動する前に、既存の書込担当が終端したことを確認する
- 委譲先は、着手後に現れた担当外ファイルの差分を自身のコマンドの副作用と推定して
  復元・逆パッチ適用・削除しない。共有作業ツリーでは並行する別主体の変更と区別できないため、
  担当外差分は保持して委譲元へ報告し、自身の担当ファイルだけを修正する
- サブエージェントは`AGENTS.md`・`CLAUDE.md`・`.claude/rules/`配下・`agent-toolkit/`配下の規範文書を
  編集しない。例外はメイン側起動プロンプトで改訂を明示許可された場合に限る
- 委譲先は、委譲された範囲を超える不可逆操作（`git push`・削除・投入先を自ら決定する
  フィードバック投入等）を実行せず、当該操作と対象を完了報告で委譲元へ返却する
- 委譲先は、依頼文と適用中のスキル規則が矛盾する場合は着手せず、矛盾の内容を呼び出し元へ差し戻す。
  認証・権限の不足または無効な呼び出しにより次の工程へ進めない場合も、独自の回避策を試みず差し戻す
- 委譲先は、指示されたコマンドが想定外の結果（契約上期待される出力の欠落を含む）を返した場合に即時に報告する。
  報告後はタスク契約に沿って事象・期待値・実際値・発生条件・直接的原因を調査し、
  技術的に成立する次の工程を継続する。次の工程が存在しないと確定した場合に限り差し戻す

## プロセス終了の安全規定

プロセスを終了させる操作は、自身が起動して識別子（PID）を確認したものに限る。
パターン一致で該当プロセスをまとめて終了させる操作（`pkill`・`killall`・`pkill -f`等）は、
対象の所有権を確認できないため実行しない。

委譲先・背景ジョブの完了を待つ手順と観測手段は、`agent-toolkit:delegation`の
`references/waiting-and-monitoring.md`が定める。

## 識別子・環境変数

配布物完結の環境変数は`AGENT_TOOLKIT_<PURPOSE>`形式とする
（代表例は`AGENT_TOOLKIT_PRIVATE_NOTES`。`atk mq`管理repoのroot、既定`~/private-notes/`）。
個人環境完結は`DOTFILES_`を使う。個別の環境変数の一覧と用途は
`agent-toolkit:agent-standards`の`references/claude-hooks.md`が扱う。
`agent-toolkit/rules/`・`agent-toolkit/agents/`配下はサブディレクトリを設けずフラット構造を保つ。
