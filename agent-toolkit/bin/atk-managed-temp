#!/usr/bin/env bash
set -euo pipefail

self="$(readlink -f "${BASH_SOURCE[0]}")"
plugin_root="$(cd "$(dirname "${self}")/.." && pwd)"
helper="${plugin_root}/scripts/_managed_temp.py"

exec uv run --no-project --script "${helper}" "$@"
