# レビュー収束ループの調整

レビュー収束ループの調整主体だけが適用するループ運営の正本である。
レビュー担当の点検とレビューイーの採否・修正は各役割の正本へ委ね、本書へ記載しない。

## 役割と反復構造

調整主体は、初版又は実装差分の入力を確定し、レビュー表を初期化し、各ラウンドの起動・応答・収束を調整する。
調整主体はレビュー担当の評価やレビューイーの採否を代行せず、正本の状態と各主体の完了を検収する。

1ラウンドは、表の構造検証、レビュー担当の起動、レビュー指摘の追加、レビューイーの全行応答、strict検証で構成する。
未解消の指摘は次ラウンドの行として同じ表へ追加し、過去ラウンドの行を変更しない。
計画レビューでは計画ごとの表を、実装レビューでは対象となる1つの`review.tsv`を全ラウンドで使用する。

## ラウンドと表のライフサイクル

調整主体は初回ラウンドの前に`atk review-table init <レビュー表>`を実行し、直後に`atk review-table validate --allow-unanswered <レビュー表>`を実行する。
各ラウンドのレビュー担当起動前には前ラウンドをstrict検証し、レビュー担当へ今回の表の絶対パス、ラウンド番号、担当する`track`を渡す。
レビューイーへは同じ表の絶対パス、ラウンド番号、修正対象として確定した採用指摘の`track`集合を渡す。
調整主体は応答済みの表を再検収し、次ラウンドが必要な場合は同じ表を用いる。
全行が回答済みとなった後のstrict検証は`atk review-table validate <レビュー表>`を実行する。

独立したレビュー観点へ入力を渡す場合、各レビュー担当へ自系統以外の`track`の行や出力を渡さない。
同じ実装レビューの準拠系と盲検系は同じ`review.tsv`へ異なる`track`で行を追加する。

## 初回・再レビューの範囲

初回レビューでは、ユーザー原文、合意済み採否・除外及び成果物から対象単位を別々に構成し、各単位の正常系、境界、状態遷移・失敗復旧、外部入力・保存不変条件及び消費主体可視境界を被覆する。レビュー担当は各確認結果へ実読箇所、実行結果又はシナリオの根拠を付ける。

未走査、根拠のない`確認済み`又は理由のない`非該当`が残る場合、調整主体は初回レビューを完了として受理せず、不足範囲を同じレビュー担当へ返す。

再レビューでは、前回レビュー対象の完全OID又は版、前回以降の修正差分、採用指摘、未解決指摘、修正担当が列挙した直接影響範囲及び新設・変更契約を入力として受け取る。変更箇所と直接影響範囲だけを再走査し、未変更かつ直接影響範囲外の既存部分は再走査しない。

直接影響範囲は、修正した識別子の呼び出し元・参照元、同じ状態又はデータを消費する経路、対応テスト、生成・配布先、変更した契約の適用先から導出する。共有parser、共通状態、公開契約、生成元、配布元を修正した場合は、実際の全消費先まで範囲を広げる。文字列、見出し、目的語、混在構造、接続関係が似ているだけの箇所は範囲へ含めない。

第2回以降の新しい指摘は、直前修正による混入又は直接影響範囲内で初めて成立した欠陥だけを登録する。未変更かつ影響範囲外の既存部分で初回から判断できた問題は、新しい指摘として登録せず、初回被覆契約の不履行と不足範囲を調整主体へ返す。再レビュー対象を直接影響範囲から広げない。

## モデル解決と起動

初版レビュー、再レビュー、レビュー修正の各起動又は継続の直前に、起動主体は対象工程の`atk config get`を実行する。
実効`engine`、`model`、`effort`は`../../delegation/references/runtime-routing.md`「工程別モデル設定」に従って解決し、同じ担当・同じタスク・実効3値一致の場合だけ同じthreadを継続する。
実効値が一致しないか、担当・タスクが異なる場合は、新しい識別子へ検収済み状態を渡して起動する。
各担当文書に固有の起動点、工程キー、入力、出力及び読み取り専用契約は各文書へ残し、本書は共通の解決手順だけを定める。

## 収束判定

調整主体は初回と第2回での収束を目標とするが、未解決の実在欠陥がある限り、前回修正と直接影響範囲を入力として修正と再レビューを反復する。
全ての指摘が解消し、表のstrict検証、計画か実装差分との照合、完了条件の検収が成立した時点で収束とする。
修正が実装単位、依存、順序、公開契約、完了条件、設計のいずれかを変更する場合は軽微と扱わず、変更箇所と直接影響範囲を再レビューへ渡す。
意味を変えない誤記・用語統一・参照訂正だけが残る場合は、修正理由を記録して再レビューを省略できる。

同一箇所か同じ違反契約・変更機構が、当初のユーザー目的、現行の公開契約、再現可能な重大欠陥に対して2ラウンド連続して成立した場合は、個別修正を止め、当該目的・契約・欠陥へ実際に影響する範囲を走査したうえで再設計・簡素化・撤去を比較する。
同じ文字列、見出し、目的語、混在構造又は接続関係だけが共通する箇所は、元の目的・公開契約・再現可能な重大欠陥への影響が確認できない限り横展開の対象にしない。
比較後も同じ単位への指摘が3ラウンド連続する場合、比較案で欠陥を解消できない場合、撤去と同一内容の復元をともに観測した場合は、対象単位、各ラウンドの根拠、比較案及び確定できない理由を添えて`needs_escalation`へ返す。
