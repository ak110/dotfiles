---
name: apply-feedback
description: >
  改善提案（セッション振り返りの出力など）を受け取り、
  各提案の妥当性を批判的に検討したうえで必要な変更を適用する。
  「フィードバックがあった」「改善提案を反映」「振り返り結果を反映」などのキーワードで起動する。
# session-reviewスキルのステップ3出力を主な入力として想定するが、
# 任意のフィードバック文面を受け付ける。
---

# フィードバックの批判的検討と適用

## ステップ1: 入力の確認

ユーザーのメッセージからフィードバック項目を抽出する。
フィードバックが含まれていない場合はユーザーに提供を求める。

## ステップ2: 内容の調整

フィードバックの記述にとらわれず、各提案の追記先と抽象度を最適化する。
適用順は次のとおりとする。最初に（0）で主提案を確定し、確定した主提案に対して(a)〜(c)を順に適用する。

- (0) 主提案の確定: 提案がリカバリー手順の文書化・lintの無視指定の追加・
  例外規定の新設など場当たり対処に該当する場合の判定軸とする。
  この場合、当該事象を発生させない根本対応（コード修正・設定変更・
  自動化スクリプトの整備・CI制約の追加など）が成立しないかを最初に検討する。
  根本対応が成立する場合は根本対応案を主提案とし、元の追記提案は不採用扱いとする。
  成立しない場合は元の追記提案をそのまま主提案とする。
  本観点は`~/.claude/rules/agent-toolkit/agent.md`「品質最優先」節の場当たり対処禁止規定の具体適用に当たる
- (a) 追記先の検討: 提案が示す対象ファイルにとらわれず、最適な追記先を選ぶ
  コードに近い側（docstring・インラインコメント）や既存の関連セクションも候補に含める。
  フィードバックが既存追記そのものを否定する内容のときは、追記先候補の検討と並行して
  当該既存記述を削除候補に含める
- (b) 抽象度の調整: 特定セッションの固有事象に特化しすぎている場合は一般化する。
  逆に抽象化しすぎて実効性を欠く場合は具体化する。
  既存節の列挙へ新ケースを追加する形の提案は、列挙拡張ではなく上位概念への統合で吸収できないかを
  抽象度調整の一環として先に検討する
- (c) 規範側の再発予防案の併記検討:
  事象の根本原因が既存規範文書の曖昧さに帰せられる場合は規範への改善案も提案候補に含める
 （観点の詳細は`agent-toolkit:session-review`スキルの自己検証(e)に従う）

## ステップ3: 批判的検討

調整後の内容で、各提案が対応に値するかを評価する。

- (d) 既存記述との重複・矛盾: 追記先ファイルの既存記述と重複・矛盾しないか確認する
  既存記述の修正・補強で対処できる場合は新規追加を避ける
- (e) コンテキストコスト対効果: 追記による恒常的なコンテキスト消費に見合う再発防止効果があるか
  単発・偶発の事象や、レビューで容易に是正できる事象は除外する
- (f) 適用範囲との整合: 反映先の規範文書が対象とする適用範囲
 （クロスプロジェクト／プロジェクトローカル／言語横断／言語固有など）と提案文の抽象化粒度が整合するかを
  書き込み直前に再確認する。クロスプロジェクト規範スキルへ言語固有・フレームワーク固有事項を追記しない

## ステップ4: 検討結果の提示と適用

各提案の採否判定と理由をユーザーに提示する。

```markdown
## 検討結果

### 採用（原案のまま）
- 対象ファイル — 提案内容

### 採用（修正あり）
- 対象ファイル — 修正後の内容 — 修正理由

### 不採用
- 提案内容 — 不採用理由
```

ユーザーの承認を得た後、`EnterPlanMode`でplan modeへ移行してから`agent-toolkit:plan-mode`スキルに従い計画を作成して実行する。
