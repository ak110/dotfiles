---
name: plan-impl
description: >
  計画ファイル合意後の計画実行スキル。
  `ExitPlanMode`直後、計画ファイルがある場合に必ず最初に呼び出すこと。
# 編集時の注意点:
# このスキルはplan-modeから続けて使われる想定ではあるが、コンテキストがクリアされている可能性があり、
# エージェントがplan-modeの内容を知らない状態で処理を始める可能性があることに注意。
# また、サブエージェント(../agents/plan-*.md)の中身もメイン側では読み込まないため、
# サブエージェントの概要などはこのファイル自体で説明が必要であることにも注意。
# また、全項目完遂・段階化先送り禁止・自動コンパクション継続前提の方針は
# 01-agent.md「品質最優先」節「セッション分割・別計画化は禁止する」上位原則と意図的重複。
# 計画実行スキルで強調するため再掲している。文面を変更する場合は両方の整合を取ること。
# 計画本文を最終確定形へ更新する規定はplan-modeスキル配下references/plan-file-guidelines.mdの
# 「進捗ログ」節と意図的に重複させている。文面を変更する場合は両方の整合を取ること。
# サブエージェント起動プロンプトテンプレート内の`## 完了報告要件`節は
# `../agents/plan-implementer.md`の`## 出力`節のchanged欄書式説明と意図的に重複させている。
# 文面を変更する場合は両方の内容を意味的に一致させること
# （冒頭句の主語は両ファイルで意図的に異なるため文字通りの同一化は誤り）。
---

# 計画後の計画実行

`ExitPlanMode`後にコンテキストがクリアされた状態で呼び出される可能性がある。計画ファイル自立性を前提とする。
計画ファイルの`## 実行方法`のレビューステップを読み取り、「レビューは実施しない」と記載されていればレビュー工程をスキップする。
それ以外は記載のスキル・エージェントへ引き継いでレビューを実施する。

`## 変更内容`に列挙された全項目を初回実装で完遂する（「初回必須」「将来拡張」など段階化ラベルを付与した先送りは禁止する）。

`ExitPlanMode`によるユーザー承認は、計画ファイル全文の精読を保証しない。
ユーザーは`## 背景`・`## 対応方針`を中心に確認したうえで承認している場合が多く、
`## 変更内容`・`### エージェント判断`の細部や周辺対応までは未読の可能性がある。
実装段階で計画ファイル本文が判断指針（`01-agent.md`「コーディングエージェント（あなた）の振る舞い」節、
特に「品質最優先」「ユーザーとともに考える」）と衝突する場合の扱いは同節に従う。
plan-impl固有の差分として、ここでの衝突は計画ファイル本文の明示記述と判断指針の明示記述が
文面上矛盾する場合に限る。自己の不安・疑念・主観的な違和感・自己推定上の制約は衝突に該当しない。
衝突を発見した時点で実装を止め、計画ファイル本文と判断指針のいずれが正かをユーザーへ確認する。

`plan-impl`実行中にユーザーから新たな割り込み指摘・是正要求が発生した場合の扱いは、
`01-agent.md`「ユーザーとともに考える」節（割り込み指摘の即時報告・恒久化検討の並行実施、
工程4「恒久化検討」の即時実施）に従う。
plan-impl固有の差分として、恒久化候補の確定文面は計画ファイルの`### 恒久化・リファクタリング内容`へ追記する。
割り込み指摘自体を「次セッションへの先送り」「`session-review`・`feedback`への先送り」として残置しない。

`ExitPlanMode`はユーザー承認応答の取得まで含めてワンステップで完結する。
承認応答を受領した時点で実装着手の合意が成立しているため、
「実装に進めてよいか」等の追加確認文を発さず、直ちに以下の工程1へ着手する。

以下の手順は省略不可の工程である。
列挙外の場面でも`01-agent.md`「品質最優先」原則に従う。

1. タスク分解
2. 実装
2.5. サブエージェント完了報告の検収
3. 検証
4. コミット
5. レビュー（レビューステップでレビュー実施が指定されている場合のみ。記載のスキル・エージェントへ引き継ぐ）

各フェーズの区切りの節目（1タスク完了・1コミット作成直前・レビュー指摘反映の節目など）ごとに、
計画ファイルの`## 進捗ログ`章へ`### <ラベル>`見出しと箇条書きで追記する。
書式詳細は`agent-toolkit:plan-mode`スキル配下の`references/plan-file-guidelines.md`の「進捗ログ」節に従う。
いずれのフェーズでも、計画ファイル本文（`## 変更内容`・`## 対応方針`等）の記載と乖離する確定形を採用した場合は、
`## 進捗ログ`への追記だけでなく当該本文を最終確定形へ更新する。
適用対象にはレビュー指摘対応によるdiffコードブロック内文面案の意味的変更も含む。
また`scratchpad`で改訂後最終形を構築した結果が計画文の
`### エージェント判断`記載のトリム内訳・抽出方針と乖離する場合も含み、
`scratchpad`完成時点（本ファイル反映前）で計画文を更新する。
`## 変更内容`配下の対象ファイル一覧の追加・除外・改名
（既存違反の同時修正・周辺対応の事後組み込み等）も乖離扱いに含み、
対象ファイル一覧のチェックボックス項目と各ファイル変更方針の`###`見出しを同時に追従して更新する。

実装着手前に計画ファイル本文の段取り記述（`## 対応方針`配下の`### 改訂作業の段取り`等）を読み返し、
「記録する」「反映する」等の進捗ログ追記要件を抽出してフェーズ末の記録対象に組み込む。
計画ファイル本文側で明示した個別の追記要件は、上記のフェーズ節目記録と並行して履行する。
計画ファイル本文の自立性が成立しているかの愚直読解は
`agent-toolkit:plan-mode`スキル工程7で`naive-executor`サブエージェントが担当する。
実装段階の本スキルでは再実施しない。

## 1. タスク分解

計画ファイルを起点にタスク分解し、`TaskCreate`で登録する（検証・コミット・レビュータスクも含む）。
過度な細分化は避ける。ファイル削除（`rm`等）はサブエージェントへ委譲しない。
レビュータスクは観点ごとに別サブエージェントを起動する（実装の経緯を持ち込まないため）。

## 2. 実装

分解したタスクを下記「実装サブエージェント委譲（plan-implementer）の判断指針」に従って実装する。

### 実装サブエージェント委譲（plan-implementer）の判断指針

計画確定後の実装は`plan-implementer`への委譲を優先する。メイン直接実装は下記手順2の判定を満たすタスクに限る。
委譲可否は自己推定上のタスク規模・対象範囲・所要量で判断せず、
タスク内容の確定度（修正方針・対象ファイル・期待結果の明確さ）で判断する。
タスクごとに次の手順で委譲可否を決める。

1. タスクが修正方針自体の判断（opus相当）を要するか見積もる
2. opus相当の判断を要するタスクのみメイン直接実装を選ぶ（品質担保のため）
3. それ以外は`plan-implementer`へ委譲する。委譲時の`model`引数と`effort`引数は次の基準で選ぶ
   - コーディングエージェント向け文書全般（`AGENTS.md`・`CLAUDE.md`・`.claude/rules/`配下・
     `.claude/skills/`配下・`.chezmoi-source/dot_claude/`配下・`agent-toolkit/rules/`配下・
     `agent-toolkit/skills/*/SKILL.md`・`agent-toolkit/skills/*/references/`配下・
     `agent-toolkit/agents/*.md`など）の改訂を含むタスク、
     および既存記述との整合維持を要する改訂タスクは`model: opus`と`effort: medium`を指定する
     （sonnetでは既存記述の並列要素の脱落や意味変質が起きやすいため）
   - 定型パターンの反復（`frontmatter`値のみの変更や機械的な単純置換など）は
     `model: sonnet`と`effort: medium`を既定とする
   - 純粋な機械的置換のみのタスクは`model: haiku`と`effort: medium`を指定する
4. 委譲したタスクが`needs_escalation`を返したらメイン直接実装へ引き上げる
5. 委譲タスクのうち、編集対象ファイルが独立する2件以上は`run_in_background=true`で並列起動する

タスク内容の確定度に迷うタスクは手順3に従ってまず委譲する。
検証タスクを委譲する場合は、対象検証コマンドを起動プロンプトの`## タスク`欄へ明記する。

### サブエージェント起動プロンプト

```text
以下のタスクを{実装 | 修正再実装}してください。

- 計画ファイル: `{呼び出し元から渡された計画ファイルパス}`
- 編集対象の文書種別: {コーディングエージェント向け文書 | 一般ドキュメント | コード・テストコード}
  同欄は`plan-implementer`側の規範スキル読込条件分岐に使用される
- 担当範囲区分: {全項目担当 | 部分担当（範囲: <明示>）}
  同欄は`plan-implementer`側の`completed`返却条件判定に使用される

## タスク

{タスクの具体的な記述}

計画ファイル本文の`## 変更内容`該当節を1文以上引用したうえで実装する。
複数の共通境界を候補列挙している計画では、いずれか1つに絞らず実装対象として明記された全候補を実装し、
テスト影響がある場合はメインへエスカレーションする。
本タスク範囲は段階化・将来拡張等のラベルを付けた先送りをせず全件完遂すること。
一部のみ実装して`completed`を返さないこと。

## 修正指摘（修正再実装時のみ）

{レビュアー指摘テキストとメインが具体化した修正方針（対象ファイルパス・該当位置・修正方針）を
 まとめて記述する。具体化方針は欄内に統合し欄外に分散させない}

## 完了報告要件

`changed`欄では、計画ファイル`## 変更内容`の各タスク項目（親バレット）に対し、
当該項目に含まれる個別指示と対応箇所を1件につき1サブバレットの形で展開し、必ず1件以上列挙する。
個別指示はフィードバック番号・修正指摘番号・独立した変更要求等を指す。
対応箇所は親バレットの`path/to/file`が変更ファイルパスを担い、サブバレットが該当節名・行範囲を担う。
未着手項目では親バレットが`未着手（needs_escalation時のみ）`を保持する。
サブバレットは未着手時点で予定する対応箇所を記述する。
レビュー指摘対応等で実装差分が計画ファイル本文diff案・`### エージェント判断`の記述と乖離した場合、
`changed`欄の別サブバレット（`[乖離]`ラベル付き）で乖離箇所と実装最終形を明示する。
```

起動元は上記の`[乖離]`サブバレット報告を確認したのち、メイン側で計画本文の同期更新を実施する。

サブエージェント起動プロンプトでは、計画ファイル本文の`## 変更内容`と矛盾する追加指示を書かない。
実装意図の追加補足が必要な場合は、委譲前に計画ファイル本文へ反映してから委譲する。
既存記述の維持範囲・追記位置の詳細指定など、委譲プロンプトが計画ファイル本文を上書き解釈する形の指示は、
plan-implementerが計画ファイル本文と委譲プロンプトの齟齬で判断分岐する構造となり計画ファイル自立性を損なう。

## 2.5 サブエージェント完了報告の検収

サブエージェント完了報告を受領した時点で、次のレビューまで保留せずメインで即時に検収する。
unplanned項目が含まれる場合はメインが採否判定する。
unplanned項目とは`[CLAUDE.md追記候補]`・`[即時相談候補]`・`[該当外]`のいずれかのラベルを持つ計画外の発生事項を指す。

- 完了報告のchanged一覧が計画ファイル`## 変更内容`当該節の独立変更項目（複数行・複数識別子の更新指示等）を網羅するかを照合する
  - 1ファイル内に複数箇所の更新指示がある場合は一部のみ更新して報告するケースがあるため`git diff`で差分実体を確認する
  - 計画ファイル`## 変更内容`の対象ファイル節から個別指示項目
   （テスト追加・`docstring`更新・`assert`追加・命名変更等）を抽出してチェックリスト化する
    - 各項目について実装差分（`git diff`実体）に該当変更が含まれることを1対1で照合する
    - 完了報告の`changed`欄が概念レベル（「既存テストのリネーム」等）で集約され
      項目抽出が困難な場合は、計画ファイル指示と実装差分を直接照合する代替経路を採用する
- 完了報告の`changed`欄で、起動プロンプト`## 完了報告要件`に従い各タスク項目の個別指示と対応箇所が
  1件につき1サブバレットの形で展開されているかを照合する
- unplanned項目が含まれる場合は計画方針および判断指針へ照らして即時採否判定する
- 採否を保留すべき事項のみユーザーへ確認する
- 修正再実装の検収では、修正差分（`git diff`実体）が新規問題の混入を生じていないかも事後点検する。
  観点は`agent-toolkit:careful-review`「指摘の統合と修正依頼」節の修正案の採用前検証と同一とし、
  当該節を典拠とする
- 完了報告で計画本文と異なる担当ファイル・識別子・命名規約が採用されたことを検出した場合、
  unplanned項目採否判定と同一タイミングで、計画本文を同期更新する
  - 同期対象は`## 変更履歴`・`## 変更内容`・`### エージェント判断`
  - 同期更新はメイン側の責務とし、後続レビュー工程へ持ち越さない

## 3. 検証

全実装タスク完了後に検証する（実装タスクへの統合も可）。
検証コマンドは計画ファイルの`## 実行方法`の検証ステップの対象範囲全体。

## 4. コミット

計画ファイルの`## 実行方法`のコミットステップに従ってコミットする。

## 5. レビュー

計画ファイルの`## 実行方法`のレビューステップに記載されたスキル・エージェントへ引き継ぐ。
複数のスキル・エージェントが併記されている場合は並列起動する。
対象範囲は計画全体（全コミット完了後の差分）、計画ファイルパスは本計画のものを渡す。
不変参照点（`origin/<base-branch>`相当または計画着手前SHA）と一時ファイル一覧を併せて渡す。
以降の起動・指摘統合・修正再実装・amend統合・再レビューは引き継ぎ先の手順に従う。
