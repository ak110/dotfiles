---
name: agent-standards
description: >
  コーディングエージェント向け文書（`AGENTS.md`・`CLAUDE.md`・`.agents/`配下・
  `.claude/rules/`・`.claude/skills/`・hooks関連ファイルなど）の
  新規作成・修正・計画・レビュー時に`agent-toolkit:writing-standards`と必ず併用して呼び出す。
# 編集時の注意点:
# 一般ドキュメント品質方針はwriting-standardsが担当する。
# 本スキルはコーディングエージェント向け文書固有の追加事項のみを扱う補完スキル。
# Claude Code固有事項は専用subsectionへ集約し、汎用部分と区別する。
# 単独では不足するため、必ずwriting-standardsと両方読み込む前提で記述する。
---

# コーディングエージェント向け文書品質

コーディングエージェント向け文書固有の記述原則。
一般ドキュメント品質方針は`agent-toolkit:writing-standards`が担当し、本スキルと併用する。

## 適用範囲

対象はコーディングエージェント向け文書一般に共通する記述原則とする。
特定のコーディングエージェント実装に固有の機能・制約・ツール名に依存する記述は本文に直接書かず、
`## Claude Code固有事項`配下などの実装固有節へ分離する。

## 読者像

対象文書はコーディングエージェントが直接読み込む。人間の読者を想定した説明文・前置き・装飾は書かない。

## 共通の記述原則

以下はagent.md「品質最優先」「日本語の品質を保つ」原則の具体例である。網羅規定ではないが、列挙外の場面でも原則に従う。

### 記述の簡潔さ

- 各行について「削除するとコーディングエージェントの判断が一貫しなくなる」と言えなければ削除する
- 同じ趣旨を複数の表現で繰り返さない。ただし文章のリズム・文意の遠近感に寄与する反復・接続は保持する
- コーディングエージェントが既に知っていることや、ツール経由で調べればすぐ分かることは書かない
- 既にコンテキストに読み込み済みと想定されるルールやスキルに対する言及を繰り返さない
  - 対象は自動ロードされる`AGENTS.md`・`CLAUDE.md`・`.claude/rules/`配下、呼び出し済みスキル内の自ファイル間参照、
    サブエージェント定義のfrontmatter `skills`でプリロードされるスキル
  - 「詳細は～スキルを参照」などの表現は避ける（読み込み済みの内容への参照誘導は冗長なため）
    - ただしサブエージェントが独立コンテキストで読む場面（スキル本文・サブエージェント定義）は参照先スキル名・節名のファイルパスを明示する
  - 自動ロード対象外の外部ドキュメント（公式ドキュメント・他リポジトリのファイル・未ロードのMarkdownなど）へのリンクは対象外

### 文書サイズ上限

対象はコーディングエージェントが本文を直接読み込む文書全般
（`agent.md`・`claude-code.md`・各`SKILL.md`・サブエージェント定義・スキル配下の`references/`など）。
本文の物理行数はソフトリミット200行・ハードリミット220行を基準とし、frontmatterおよびYAMLヘッダ行も行数に算入する。
コーディングエージェントによっては文書先頭の一定行数（観測例: 先頭220行までしか読み込まない実装）のみを読み込むため、ハードリミット超過は重要な指示の読み込み範囲外への漏れを招く。
ソフトリミット200行は計画ファイル作成までの段階の組み立て基準、ハードリミット220行は実装以降の評価基準とする。
計画段階では200行以内を厳守し、ハードリミット220行を下回ることを計画段階での超過許容理由としてはならない。

- ソフトリミットに近づいたら「### 記述の簡潔さ」の縮減指針に従い、上位原則から自明に導出できる記述・
  他文書に正規版がある記述・公式ドキュメント1-fetchで代替可能な記述を優先的に除去する
- 改訂の計画作成時は、改訂後の物理行数を計画段階で試算し本規定への適合を事前確認する
  - 計画段階の試算は追加行数・削減行数の加減算ではなく、改訂後の最終形に近い文面を組み立てて`wc -l`で実測する
  - 計画段階で対象ファイルの現行行数を`wc -l`で測り計画ファイルの`## 調査結果`へ記録し、改訂後の試算値を`### エージェント判断`へ記載する
  - 計画ファイルの`## 変更内容`へ改訂後の最終物理行数の目標値（ソフトリミット200行以内維持等）と実装段階での`wc -l`確認手順のみを記載し、追加行数・削減行数の内訳数値は書かない
  - 既存ソフトリミット超過時または改訂で超過する場合は、既存節縮減を本計画スコープへ含めて200行以内へ収める
  - 試算時の文面候補は計画段階で機械チェック適合形を確定してから`wc -l`実測する
    - `agent-toolkit:writing-standards`配下`scripts/check_line_width.py`で127幅超過の有無を検査する
    - `uvx pyfltr run-for-agent --no-fix`でtextlintの`sentence-length`違反等を検査し計画段階で解消する
- 改訂で既存節縮減を含む場合、縮減対象の節名と縮減根拠を計画ファイルの`## 変更内容`へ明記する
  - 縮減根拠は4類型: SSOT違反・自明導出・公式ドキュメント代替可能・上位指針への統合（`trim-agent-docs`委譲時も同じ）

### メタ記述の禁止

スキル・エージェント・ルールなどコーディングエージェントへ直接読み込まれる文書は、エージェントへの直接指示として書く。
編集経緯・SSOT宣言・履歴メモなど実行判断に寄与しないメタ情報は本文に書かず、frontmatter内コメント（`#`行）に書く。
他スキルとの併用関係・優先順位・適用範囲・呼び出し関係など、実行判断に直接寄与する関係性情報は本文へ書いてよい。

「本スキル」「本ドキュメント」などの自己言及そのものは許容する。
本文へ書かずfrontmatter内コメントへ書くべきメタ記述は以下の4種類。

- 管理方針 — ドキュメント自身がどこに何を集約しているかを語る言い回し
  - 違反例:「〜をSSOTとしてまとめている」「本スキルでは再掲しない」「対象をまとめる」
  - 改善例: 本文には集約後の内容をそのまま直接指示として書く（読み手は内容自体から理解する）
  - 対象外:「（手順1で読み込む）」「（本スキルに先立って読み込む）」のような使用タイミング注記は、
    コーディングエージェントの実行手順を直接示す情報のため許容する
- 編集判断 — 書き手がどのように記述を取捨選択したかを語る言い回し
  - 違反例:「〜のSSOT」「本節では〜を記述する」「以下に〜の規定を列挙する」
  - 改善例: 必要な指示はそのまま本文に書き、選択理由は本文に含めない
- 運用変化 — 過去・現在の状態の変化や廃止・移行を語る言い回し
  - 違反例:「〜へ集約済み」「以前は〜だった」「判定ラベルは廃止」「旧仕様では〜」
  - 改善例: 現在守るべき運用・現在のフォーマットを直接記述する
- 編集者向け同期手順 — 修正後に他ファイルへ反映する手順、編集経緯、履歴メモ、
  SSOT違反承知の重複再掲宣言など、編集者向けに残すメタ情報
  - 違反例:「修正後に他ファイルへ反映する手順」「本原則は〜と同趣旨」
  - 改善例: 編集者向けメモはfrontmatter内コメントへ移し、本文には現行の規範のみを残す

### 横断指針の配置

複数フェーズ・複数文脈に跨る指針は独立節として手順リスト直後など適用範囲が明確な位置に置く。
特定フェーズ・特定文脈の見出し配下では、コーディングエージェントが見出し階層から限定指針として狭く解釈する。

### 適用範囲・条件の明示

規範を新規作成・改訂する際は、当該規範の適用範囲・除外対象・前提条件を明示する。曖昧なまま規範化しない。

- 複数のディレクトリ・モジュール・配布物が並ぶ文脈で規範を書く場合、どの対象に適用しどの対象に適用しないかを明示する
- 配布物（プラグイン・ライブラリ）と非配布物（個人環境・専用設定）が混在する場合は配布物境界を明示し、
  非配布物固有の依存を配布物側へ持ち込まない原則を併記する
- 親項目が複数の対象を束ねるサブ項目を持つ場合、サブ項目の適用対象が親項目と一致するか・狭まるかを明示する
- 適用範囲が後から拡張・縮小する余地のある規範は、改訂時に範囲表現を再確認する
- 規範を新規作成・改訂する際の適用範囲は、最も広く取れる範囲を初期案とし、
  対象を限定する場合は限定の根拠を本文に併記する。
  狭く取った結果として限定理由を書かないまま放置すると、後続の改訂で対象漏れが発生する
- 規範本文は独立コンテキストで参照されるため、追加文面案に
  会話文脈依存の指示語（「今回」「先ほど」「当該セッション」など）を含めない。
  具体対象を確定できる名詞句で記述する

### サンプル・テンプレート本文の純度

サンプル計画ファイル・テンプレート・記述例コードブロック内には、成果物本文に書くべき内容のみを置く。
書き方の手引き（メタ説明文）をサンプル本文に混入させると、
コーディングエージェントが手引き文ごと成果物へ転記する誤読を招く。
手引きはサンプル外（節見出し配下の地の文・別節）に分離する。

### コンテキスト汚染の回避

- 悪い例の記述可否は、コンテキスト汚染リスクの有無で判断する
  - 汚染リスクが高い悪い例（口語の検出語・俗語・禁止パターンの具体的な語句など、生成確率を上げる語彙）は本文に書かず、
    `references/`配下の隔離ファイルへ置く。本文で具体例が必要な場合は抽象化した表現にとどめる
  - 汚染リスクが低い対比例（語順・主述・構成・冗長性など、危険な語彙に依存しない構造レベルの良否）は本文に直接書いてよい
- 機械チェック用辞書（NGワード・正規表現リストなど）の検出語そのものを、
  スキルの本文・テストコード・hookの警告メッセージへ転記しない（生成確率を上げる逆効果になるため）
- `references/`配下の隔離ファイルは、危険語彙・禁止パターンを含む悪い例の格納専用とする
  - メインエージェントから直接読み込ませない（確認はExploreサブエージェント・修正は`plan-implementer`経由）
  - サブエージェントは悪い例文面を成果物・指摘・出力へ転記しない
  - 機械チェックスクリプトの検査対象から除外する

### 既知情報・冗長記述の排除

- 学習データやシステムプロンプトで既知の一般指針は書かない
- システムプロンプトと矛盾しない、かつ重複しない指示にする
  システムプロンプトで定義された用語を優先し、訳語が揺れる場合は原語を使う。
  やむを得ず矛盾する指示を書く場合はその旨注記する
- 細則の列挙はそれを束ねる上位指針に集約する
  詳細手順は対応するスキルのreferences配下へ退避し、トリガー時のみロードする
  - 既存節の列挙へ新ケースを追記する提案があった場合は、列挙の拡張ではなく上位指針への昇格・統合で
    吸収できないかを先に検討する。同種事象を含む上位概念へリライトできる場合は、列挙拡張を選ばない
- 同一概念を複数箇所で長文展開しない
  用語定義は1箇所で短く示し、他箇所では用語のみで参照する。
  複数ファイルで同一トピックを扱う場合は一箇所を正規の定義元とし、他のファイルは参照に留める
- 短い権威表現（用語・規範用語）で長文説明を置き換える場合、
  独立した第三者のコーディングエージェントが用語1語のみから同一行動を取れるかを確認する。
  エージェントの行動に影響する規範の置き換えではサブエージェント検証を推奨する
- コーディングエージェントが当然実行・解釈できる手順詳細（読み取り・処理順序・判断ステップ等）を
  1段ずつ書き下さず、目的・対象の指示にとどめる

## タスク固有で読み込む補足情報

- `references/agent-skills.md`: スキル編集時
- `references/claude-hooks.md`: hook・hookから呼び出されるスクリプトの編集時（Claude Code固有）
- `references/auto-mode.md`: Claude Code auto modeのカスタムルール編集時（Claude Code固有）

## Claude Code固有事項

Claude Code固有の機能・制約に依存する記述。
他のコーディングエージェントを利用する場合は読み替える前提とする。

### `.claude/rules/`配下のpaths活用

`.claude/rules/`配下のルールファイルはYAML frontmatterの`paths`フィールドで条件付きロードできる。
パターンに一致するファイルをClaude Codeが読むときにのみコンテキストへロードされ、起動時の常時ロードを避けられる。
パスパターン記法と詳細はMemory公式ドキュメントを参照する。

新規ルール作成・既存ルール見直し時は以下を順に評価する。

- 常にコンテキストに必要かを判断する。タスク固有で読み込み頻度が低いものは`.claude/skills/`配下へ移す
- 特定モジュール・特定ディレクトリ群でのみ参照すれば十分なものは`paths`で対象を限定する
- 全コード・全ドキュメントに横断的に関わるものはfrontmatter無しのまま残す（無条件ロード）
- 既存ルールへ節を追加する場合、追記内容の適用範囲（対象ファイル群・ディレクトリ群）が
  `paths`の対象範囲と一致しているかを確認する。
  対象外まで広がる場合は別ルールへ分離するか`paths`を見直す

paths未指定のルールは`CLAUDE.md`と同等の優先度で起動時に常時ロードされ、コンテキストを継続的に消費する。

### 公式スキル・リファレンス

スキルの新規作成・hook実装など該当する作業では、
公式マーケットプレイス（`anthropics/claude-plugins-official`）の関連スキルを参照する。

- `skill-creator:skill-creator`
- `plugin-dev:skill-development`
- `plugin-dev:agent-development`
- `plugin-dev:hook-development`
- `plugin-dev:plugin-structure`

### 公式ドキュメント

referencesに記載のない仕様や新機能、ユーザー指示との認識相違は以下を`WebFetch`で取得する（URLに`.md`サフィックスを付与するとMarkdown形式で取得できる）。

- Memory — `https://code.claude.com/docs/ja/memory.md`（`CLAUDE.md`・`.claude/rules/`、`@import`構文、自動メモリー、読み込み順とスコープ）
- Skills — `https://code.claude.com/docs/ja/skills.md`（`SKILL.md`の作成方法、frontmatter、文字列置換、呼び出し方法、ツール制限）
- Subagents — `https://code.claude.com/docs/ja/sub-agents.md`（サブエージェント定義、配置スコープ、frontmatter、ツール・権限・モデル設定、呼び出し方法）
- Hooks — `https://code.claude.com/docs/ja/hooks.md`（Hookイベント、matcher、handler type、設定場所、入出力スキーマ、終了コード、JSON出力）
- Plugins — `https://code.claude.com/docs/ja/plugins.md`（作成手順、`.claude-plugin/plugin.json`、ディレクトリ構成）
- Plugins技術リファレンス — `https://code.claude.com/docs/ja/plugins-reference.md`
