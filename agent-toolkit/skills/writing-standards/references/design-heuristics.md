# 領域別設計観点カタログ

頻出設計領域ごとに、その領域で守るべき価値と、判断に用いる観測可能な基準を示す。
設計判断を確定する時点で、該当する発火条件の節を照合する。
`implementation-time.md`の「セキュリティ・ロギング・エラー処理」と重複する観点は
同資料側の参照に留める。

## 責務帰属（SRP）分析の観点

責務肥大化・SRP違反を点検する場面（リファクタリング検討・改善提案の主提案確定・コード設計レビュー等）で共通基準として用いる。

- 責務帰属: 対象機能の責務がどのコンポーネントに帰属すべきかを問い、現在の配置が責務上正しいかを確認する
- 直接アクセス: 他コンポーネントが対象コンポーネントの内部状態（設定ファイル・フラグファイル・内部API等）へ
  直接アクセスしている構造があるかを点検する
- 抽象化層: 利用側が低レベルな副作用（ファイル存在確認・パス直書き・shellコマンド組み立て等）へ依存している箇所があれば、
  判定API・専用サブコマンドへ集約し抽象化層を1段上げる選択肢を検討する

表層整理（内部重複の撤去のみ・既存実装の再利用のみ）を選ぶ場合も、責務帰属を一度問う（努力目標。責務の見直しを省くと、同じ配置のまま重複だけが移動する）。
戻り値型・引数順の差異のみで本体ロジックが同一なら、ラッパー化・引数化で集約する。重複した範囲が同じ理由で変更される場合は、共通化を実装前に組み込む。

## 並行処理・排他制御の設計観点

ロック・排他制御・並行処理を含む機能を設計する場面で共通基準として用いる。実装着手前に以下を確定する。

- 排他の単位と実行コンテキスト境界: プロセス・スレッドのいずれで保持を判定するかを明示する
- ロック保持中の区間設計: 人間の応答待ち・長時間処理をロック保持中に含めない
- 失敗時の共有状態復元: 開始前の状態へ戻す手順と復元自体の成否確認を用意する
- ユーザー入力を含む中間生成物の生存期間: 成功時は削除する。再試行可能な失敗時
  （ネットワーク断・タイムアウト等）は再試行に使うため保全する。内容検証・権限検証等で
  拒否した入力（形式不正・マルウェア検出等）は再試行対象ではないため保全せず、
  削除または隔離し保持期限を定める
- 対象リソース消失時の挙動: リソース不在・削除済み時の動作を確定する
- 変更前の版が並行稼働する可能性: 常駐プロセス・別端末・別セッションから同一の共有リソースを
  操作する成果物では、更新後も変更前の版が稼働し続ける期間があると想定する
  - 変更前の版が生成する状態（旧構造のディレクトリ・旧形式のファイルなど）を
    新しい版が受け取った場合の挙動を確定する
  - 対象がゼロ件・不完全な場合を含めてテストへ含める

## 検索・一覧画面の設計

発火条件: 検索フォーム・一覧画面・ページネーションを新規設計または改修する場面

- あいまい検索の方式分解: 完全一致・前方一致・後方一致・部分一致の4方式へ要求を分解する
  - 一般的な傾向として完全一致（`= '文字列'`）と前方一致（`LIKE '文字列%'`）はインデックスが
    使用されやすく、後方一致（`LIKE '%文字列'`）と部分一致（`LIKE '%文字列%'`）は一般に
    既定のB-tree系索引が利用されにくい。製品・照合順序次第で結果が変わるため、この対応は傾向として扱う
  - 実際にインデックスが使われるかは、対象スキーマと代表データに対する実行計画（`EXPLAIN`等）で確認する
- 想定データ件数を確認する（1万件は一般に語られる目安であり、実際の要否は対象環境の
  応答時間実測で判断する）。完全一致・前方一致で要件を満たせるか先に検討し、
  後方一致・部分一致が必須と判明した場合、`pg_trgm`（トライグラム類似度によるインデックス支援、
  `LIKE`／`ILIKE`向け）で十分か、Elasticsearch等の全文検索エンジンが必要かを別途検討する
  （両者は対象範囲が異なる別機能であり、置き換え可能な選択肢として並べない）
- ページネーション方式: 要求する操作性（任意ページへのジャンプの要否）と応答速度のどちらを優先するか確定し、実測で検証する。
  ページ番号への任意ジャンプはオフセット型が担い、カーソル型は前後移動を提供する
- 総件数表示の精度: 正確な総件数の取得は、対象件数やクエリー条件次第で全件走査に近いコストが
  掛かる場合がある。「100件以上」等の概算表示で要件を満たせるか確認する
- 並び替え項目の設計コスト: 項目ごとに対応するインデックスの設計・運用コストが掛かる。
  必要な項目に限定するか、全結果を取得済みの場合に限りフロント側ソートで代替できないか検討する
- 曖昧要求の具体化: 検索方式・ページネーション方式・総件数表示・並び替え項目の各観点で
  仕様を具体化してからコストを見積もり、DB側とアプリ側の役割分担を確定する

## キャッシュ設計

発火条件: キャッシュ層を新規導入・変更する場面

守るべき価値は、元データとの整合が要件を満たす範囲に収まることと、キャッシュ層の障害が機能の停止へ直結しないことの2つとする。
書き込み経路ごとの無効化の漏れ、キーの衝突、同時再生成の集中はいずれもこの2つを損なう。

## 非同期ジョブ・リトライ設計

発火条件: バックグラウンドジョブ・非同期処理・リトライを含む機能を設計する場面

守るべき価値は、同じジョブが複数回実行されても副作用が重複しないことと、失敗が検知できる状態に留まることの2つとする。
冪等性キーには論理ジョブ単位で不変な値（依頼元が発行するリクエストID等）を使い、実行の都度採番される値は使わない。
同時実行の防止は「並行処理・排他制御の設計観点」、リトライ対象の判断は`implementation-time.md`の「セキュリティ・ロギング・エラー処理」に従う。

## DBスキーマ・マイグレーション設計

発火条件: スキーマ変更・マイグレーションを設計する場面

守るべき価値は、デプロイ中に新旧のアプリコードが同一スキーマへ同時アクセスする期間でも両者が動作することと、失敗時に前の状態への復旧手段があることの2つとする。
既存データの変換・バックフィルの要否と移行処理の同梱は`design-time.md`「エンドユーザーデータの移行」に従う。

## API設計

発火条件: 自コードベース外の呼び出し元が存在する公開APIのエンドポイント・スキーマを新規設計または変更する場面。
同一コードベース内でのみ呼び出される内部APIは対象外とし、`agent-toolkit/rules/01-agent.md`「判断指針」の理想形優先原則に従う。

守るべき価値は、呼び出し元が自分のコードを変えずに使い続けられることと、エラーと部分失敗を呼び出し元が判別できることの2つとする。
一覧系エンドポイントには「検索・一覧画面の設計」のページネーション観点を適用する。

## 認証・認可設計

発火条件: 認証・認可・権限チェックを新規設計・変更する場面

権限チェックは、抜け漏れが起きない一元的な層へ配置し、デフォルト拒否を既定として明示的に許可した操作だけを通過させる。
権限の必要最小限化自体は`implementation-time.md`の「セキュリティ・ロギング・エラー処理」に従う。
セッションの有効期限と失効、権限変更操作自体への認可、及び可否判定の追跡手段も同じ工程で確定する。

## ファイルアップロード設計

発火条件: ファイルアップロード機能を新規設計・変更する場面

- サイズ上限: アップロード上限をアプリケーション・プロキシ・ストレージの各層で一致させる
- ストリーミング処理: 大容量ファイルをメモリーへ全展開せずストリーミングで処理できるか検討する
- 内容検証: ファイル種別の検証を拡張子だけに頼らず、内容（マジックバイト等）も確認する
- 一時領域の後始末: アップロード中間ファイルの生存期間は「並行処理・排他制御の設計観点」の
  中間生成物生存期間の原則に従う。アプリ側が生成した派生一時物
  （リサイズ後画像等の再生成可能なもの）は失敗時に削除する
- 保存先の設計: 保存先の隔離、配信時の実行防止、パストラバーサル対策の3つを、互いに代替しない独立した対策として適用する。
  保存先は公開Webルート外（署名付きURL・認可済み配信エンドポイント経由でのみ到達可能な領域）へ置く。
  能動コンテンツ（HTML・SVG等）の実行を防ぐには、安全な形式への変換（画像への変換等）か、
  ダウンロード用MIME型（`application/octet-stream`等）・`X-Content-Type-Options: nosniff`・
  `Content-Disposition: attachment`の3点の組み合わせのいずれかを必須とする。
  ファイル名・パスはアプリ側で生成する

## 案の採否と実施順の判定

発火条件: 採用案の選定、代替案の比較、確認の選択肢を構成する場面。レビュー指摘の採否では、判定の手順を`agent-toolkit:review-standards`が定め、本節はその判定が用いる費用比較の基準だけを提供する

案の採否は、成果物が継続的に負う保守コストと、その案が得る便益の比較で判定する。一度きりの実装、検証及び再レビューの作業量は、この判定の入力と案を退ける理由のいずれの対象からも外す。作業量を根拠にすると、ユーザーが求めた機能が実装規模だけを理由に成果物から除かれる。

案の説明、AWI、計画へデメリット、既知の制約、取りこぼす区間、判定できない状態、競合が起きる区間を残す場合は、確定前に、その欠陥を生じさせる側を変更して欠陥自体を解消する案を1件以上挙げて比較する。
発生源が別の主体、工程又はファイルにある場合も、その発生源を変更対象の候補へ含める。
発生源側の案を採用しない場合は、継続的な保守コストと便益の比較で理由を示す。残存欠陥の説明は、その欠陥の解消を代替しない。

- 保守コストは、恒常的な複雑性、状態管理、互換経路及び運用負担のうち、その案が成果物へ恒常的に加えるものを指す
- 委譲、待機、再試行、監視など反復して実行される機構の案を比較する場面では、その案が実行のたびに生じさせるコスト（モデル呼び出しの回数、待機の分割回数、追加のturn）を保守コストと並ぶ評価軸とする。このコストは実行回数に比例して繰り返し生じるものであり、採否の根拠から除く一度きりの作業量とは区別する
- 着手済み工程の継続・停止は`agent-toolkit/rules/01-agent.md`「完遂と先送り」が定める
- 実施すべき処置が複数あり、同一の工程内で全てを実施できない場合の実施順と実施範囲は、各処置が防ぐ損失の大きさで決める。損失の比較材料は、その事象の発生回数、1回ごとに失われる工程及び所要時間の内訳に占める寄与とする
- 採用済み要求の対象集合と完成条件は、いかなる工程上の事情があっても縮小又は緩和しない。一括処理で品質を維持できない場合は、採用済みの対象を個別に処理する。処理単位の都合を要求の範囲へ反映すると、採用した要求の一部が未実施のまま完了する
