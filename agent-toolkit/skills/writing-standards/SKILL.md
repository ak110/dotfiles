---
name: writing-standards
description: >
  Markdown・README・技術文書・API文書などのドキュメント、
  各種プログラムやスクリプトのコメントの新規作成・修正・計画・レビュー時に最初に必ず呼び出す。
# 編集時の注意点:
# コード編集時はcoding-standardsと本スキルの両方を読み込むが、
# ドキュメントのみの編集時はwriting-standardsのみを読み込むことに注意。
# コーディングエージェント向け文書固有のガイドはagent-standardsスキルへ分離した。
# 本スキルではClaude Code固有事項を扱わない。
# 対象読者と文体の基本方針はagent.md「言語表現」章に集約。
---

# ドキュメント品質

一般ドキュメント（Markdown・README・技術文書・API文書、コメント類）の品質指示。
コーディングエージェント向け文書も対象に含むが、その編集時は`agent-toolkit:agent-standards`を併用する。

以下の規範は、agent.md「品質最優先」「日本語の品質を保つ」原則の具体例である。
網羅規定ではないが、列挙外の場面でも原則に従う。

## 読み手別の追加注意点

成果物の対象読者ごとの追加注意点。

- エンドユーザー向け: 内部実装は知らない前提とし、専門用語は初出時に補足する
  - 対話型UIは操作・状態・対処を端的に伝え、エラーは原因と対処を一組で示す
  - ドキュメントは「何ができるか」「どう使うか」を中心に利用者の操作目線で書く
- 開発者向け: 前提知識を共有する開発者を想定し初歩説明は省くが、コード識別子レベルの詳細は扱わない
  - 「なぜそう設計したか」「どこに注意が必要か」を中心に書く
- コーディングエージェント向け文書: 前置き・自己紹介・書き手目線の解説は不要とし、判断に必要な情報のみ直接記述する

## ユーザー入力素材の取扱い

ユーザーが事情・経緯・背景を説明しながら作業を指示する場合、提示文面の全体を成果物へ無加工で転記してはならない。
成果物ごとの対象読者と要求粒度に応じて、要件として咀嚼すべき情報と参考に留めるべき情報を区別する。

- 計画ファイルの`## 背景`はユーザー発話・提示素材の原文転記が許容される唯一の領域
 （詳細は`agent-toolkit:plan-mode`スキルの観点Aに従う）
- 上記以外の成果物（実装コード・コメント・ドキュメント本文・コミットメッセージ・サブエージェントへの指示・
  PR説明など）には、対象読者が必要とする情報粒度へ要約・抽出してから取り込む
- ユーザーが提示したセッション固有の経緯・自己セッションでの観測事象・ユーザー個人の名指しなどは、
  恒久的な規範・公開成果物に書き残さない
- 取捨に迷う場合は対象読者の判断に必要かを基準とし、必要でない情報は省く

## 構成・構造

- 対象読者を明確にし、冒頭に目的・概要を置く
- 前提条件（必要な知識・ツール・環境）は本題の前に配置する
- 見出しの階層は3段（`##` / `###` / `####`）までを目安とする
- 関連する内容は近くにまとめ、読者が文書内を往復しなくて済むようにする
- コマンド例やコードスニペットはコピー&ペーストでそのまま動くことを前提に書く
- 外部リソースへのリンクは、リンク切れリスクを考慮して要点を本文にも記載する
- トップダウン（段階的詳細化）で構成し、追記時も末尾追加でなく既存記述との粒度・配置の整合性を確認する

## 日本語の書き方

書き方の上位原則はagent.md「日本語の品質を保つ」節に従う。

- 断定的で安定した語尾を使う（常体では「〜する」「〜である」「〜となる」「〜とする」「〜ものとする」）
- 一語で意味が通る名詞は単体で表記する（後ろに別の名詞を付け足して二語化しない）
  - 非推奨例:「設定値」「構成情報」「入力データ」「出力結果」／推奨例:「設定」「構成」「入力」「出力」「結果」
  - 単体で意味が通らない場合に限り、必要な修飾語・複合語を組み合わせる
  - 文脈で「値」「情報」「データ」を含めて初めて意味が確定する場合は二語表記でよい
   （例: 設定キーと設定値を対比する文脈の「設定値」）
- 比喩動詞を避け、観測できる事実を表す標準的な技術用語を使う
  - 推奨例:「例外を送出する」「プロセスを終了する」「設定を読み込む」「実行する」「上書きする」「無効化する」
- 危険語彙・禁止パターンを含む観点の対比例は`references/tone-examples.md`を参照する
  - 参照時は必ずサブエージェントを使用する（コンテキスト汚染回避のためメインから直接Readしない）

### 行動指示の述部

- 行動指示を終止形で結ぶと主体が不明確になり、説明文と混同されやすい
  - 常体の推奨例:「〜を参照」／敬体の推奨例:「〜を参照してください」
  - 例外: コーディングエージェント向け文書は常体の指示終止形（「〜する」「〜を確認する」）を許容する
- 同一ファイル内で述部形式が混在する場合は優勢な形式へ統一する
  拮抗時は主語・述語のそろった終止形を既定値とし、体言止めは見出し的列挙・用語定義に限定する

### 語順と主述

- 結論・要点・規定の中身を文の前方に置く。種別や分類のラベルを機械的に前置きしない
- 主語と述語を近づける（主語直後に長い括弧列挙や挿入句を置かず、サブバレットや後続文へ分ける）
- 同一の話題・対になる概念は隣接させて並べる
- 規定は動作主の曖昧な能動表現を避け、規範述語（「〜とする」「〜ものとする」）で結ぶ
- 説明文を体言止め断片の連なりで構成せず、主語・述語のそろった1文として記述する（行動指示の述部は前節に従う）
  - 例外: 用語定義の見出し（「用語名: 〜の状態」など辞書形式）は体言止めで統一してよい

## 論証品質

### 論証の厳密さ

- 推量・可能性・読者の疑念を示す述部（`かもしれない`・`だろう`・`ようだ`・`らしい`など）を
  根拠なく断定へ変えない。本文内の根拠で確定している場合に限り断定へ整える
- 複数の要因が関係する事象を単一原因に還元しない。要因別に切り分けて記述する
- 異なる対象（別々の決定・別々の原因・種類の違う問題）を同一の語でまとめない
- 因果を述べるときは「AだとBになる」とだけ書かず、機構（なぜそうなるか）を1文で添える
- 検出・保証・解決を「必ず」できるかのように書かない。
  条件付きで述べる（「〜しやすい」「〜できることが多い」「〜が成り立つ場合に限り」）
- 主張は挙げた例が支える範囲に揃える。例が一部しか支えないときは主張の範囲を例に合わせて狭める
- 「次節で扱う」のような前方参照は段落末・節末へ配置し、論証の途中に置かない。
  前方参照を設けた場合は本当に回収されるかを文書完成前に確認する
- 論を一方向に進め、結論→反論→言い直しで主張を揺り戻さない
- 譲歩や逆接で段落を閉じて結論を宙吊りにしない
- 対句否定（「AではなくB」型）には否定する命題を具体に明示し、判断の根拠を1文添える

### 読み手負荷の管理

- 後で参照しない固有名（ファイル名・関数名・識別子）を本文に載せず、一般的な語で済ませる
  - 一般的な語の例:「設定ファイル」「ファイル読み込みの補助関数」など
- 抽象語の指す内容が文脈から一意に決まらないときは、可能な範囲で同格の丸括弧挿入でその場で特定する
 （例:「対象（変更対象モジュールのうち未テスト箇所）を抽出する」）。
  読者に前を読み返させない構成を優先する
- 新しい例・場面を追加して読者が保持すべき文脈が増えるときは、
  前の例と何が違うか・なぜもう1つ必要かを前置きで示す
- 章冒頭・節導入では、これから扱う内容に関係しない過剰な詳細を書かない

## 日本語の表記ルール

textlintの`preset-jtf-style`で検査される項目は同プリセットに従う。以下は補足規定。

- 日本語と全角括弧の境界にスペースを挿入しない（例: `設定ファイル（既定値あり）`）
- 全角丸括弧は同一論理行内で開きと閉じを完結させる。リスト項目内で改行する場合は閉じてから改行する
- 全角丸括弧の入れ子は避ける。内側の補足にはカンマや読点で区切る表現を使う
- emダッシュ（`—`）・horizontal bar（`―`）・2倍ダッシュ（`——`）を日本語の地の文・見出しで使わない。
  同格・補足の挿入は全角丸括弧、言い換え・敷衍は句点で2文に分けるか読点でつなぐ
  - 範囲を示すenダッシュ（`–`）や英語の固有名詞内（`Curry–Howard`など）、コードブロックは対象外
- 英文脈の半角括弧は英数字との間にスペース1つ（例: `Python (CPython 3.12)`）
- 外来語の語尾には長音符を付ける（例:「ユーザー」「サーバー」「パラメーター」。3音以上の語は省略しない）
- 技術用語・固有名詞・コード識別子は原典の表記を優先する
- 文面案を執筆する前に`references/textlint-violations.md`を読み込み、頻出違反パターンを確認する
 （メインエージェント・サブエージェントとも）

## 改訂・保守

- 追記内容の粒度が既存記述と整合しているか確認し、必要に応じて既存部分や章構成も変更する
- 新規執筆・改訂のいずれでも、異なる節・ファイル間で相互参照するフォーマット要素
 （テーブル列名・節名・キー名など）を、執筆完了前に相互に照合して整合を確認する
- 陳腐化した記述は積極的に削除する。「古い情報が残る」ことは「情報がない」より有害
- SSOTを維持する。重複が避けられない場合はどちらが正かを明記する
- ドキュメントの変更がコードの変更と連動する場合は、同じコミットまたはPRに含める
- 本文から数えて把握できる件数・列挙（「N件」「N観点」「N項目」など）は明記しない
  仕様書の網羅性チェックなどで意図的に明示する場合は、本文との整合を保つ責務を負う
- 本文と連動する従属箇所（フォーマット例・コードブロック・目次・件数表記・要約・mermaid図のラベル等）は、
  本文変更時に合わせて更新し片方だけ更新の矛盾を残さない
- 推奨例・違反例リストへの語の追加・削除を伴う改訂では、
  同一ファイル本文中の当該語の使用も`grep`で同時走査して自己整合を確認する

## コメントの記述粒度

- ソースコードには必要最小限のコメントを書き、コードから自明な処理にコメントを付けない
  書く場合は「なぜそうするか」（設計判断の背景・閾値選定理由・トレードオフ・バグ回避理由）か
 「何をしているか」（数行以上の塊の短い要約・非自明な制御フロー）に限る
- 実装詳細はインラインコメントに書き、モジュール冒頭のdocstringへ集約しない（冒頭は役割・概要のみ）
- 同概念を扱う複数の関数で説明が重複する場合、本質的な説明は呼び出し関係の入口側1箇所に集約する
  内部関数側は1行サマリーか入口関数への参照で済ませる
- ドキュメントはコードからわからない情報や意図・設計理由を書いて現在の状態を直接記述し、
 「〜へ集約済み」「〜へ移行済み」のような時系列表現は使わない（履歴はコミットメッセージに委ねる）

## 口語表現チェック

口語的な日本語表現の混入を防ぐため、`scripts/check_colloquial.py`の実行を必須とする。

```sh
uv run --script path/to/writing-standards/scripts/check_colloquial.py path/to/file.md
```

- 検出範囲: `.md`・`.py`・`.txt`・`.yaml`・`.yml`・`.toml`（コードブロック内のコメントも含む）

コンテキスト汚染を避けるため、以下のファイルはメインエージェントから直接Readしない。

- `agent-toolkit/skills/writing-standards/references/tone-examples.md`
- `agent-toolkit/scripts/_colloquial_words.txt`

確認が必要な場合はExploreサブエージェント経由で参照し、修正が必要な場合は`plan-implementer`経由で行う。

口語禁止語を計画ファイル本文・PR説明・チャット転記等で議論する場合は語そのものを書かず、
`references/tone-examples.md`の節名（比喩動詞・主観評価語など）で間接参照する。

LLM口調（予告と総括・正面から系・空虚な形容・空虚な動詞・接続の型・弱い緩和と称賛）の
禁止語彙・対比例は`references/tone-examples.md`の「LLM口調」節に従う。

## Markdown記述スタイル

Markdown要素はセマンティック（意味的役割）に沿って使う。
視覚効果や機械チェック違反の回避を目的に、本来の意味と異なる要素を使わない。
行幅・句点位置などの細則はmarkdownlint・textlintで自動検証される。
頻出textlint違反パターンは`references/textlint-violations.md`を参照する。

セマンティック原則の具体例:

- 強調（`**`）は語句強調にのみ使う。見出し代替として使わない。見出しが必要な場合は`###`等の見出しを使う
- インラインコード（バッククォート）をtextlintの違反回避手段として使わない。違反が出る場合は表記そのものを見直す

機械検証規範:

- markdownlintが通るように書く。特に注意するルール:
  - `MD022`: 見出しの前後に空行を置く／`MD031`: フェンスドコードブロックの前後に空行を置く
  - `MD040`: フェンスドコードブロックに言語を指定する
- 1行の表示幅は半角換算で127を上限とする（全角=2、半角=1。例外はコードブロック・表）
  1文ごと（`。`ごと）に改行し、`scripts/check_line_width.py`で検査する
- 箇条書きは1項目1文で書き、複数文は別項目分割かサブバレット階層化で表す
  - 例外: 規則文と同一論点の補足・条件・例外を直結する場合に限り、句点で接続した2文を許容する
  - 120字超の単文や半角識別子・コマンド名を含む列挙は最初から別項目化・サブバレット化で予防する
  - 各項目の末尾に`。`は付けない（`preset-jtf-style 1.1.3.箇条書き`の`shouldUsePoint: false`規定に整合）

その他:

- 図はMermaid記法で書く
- 別のMarkdownファイルへのリンクは用途で書き分ける
  Markdownソースのまま読まれる想定（`README.md`・GitHub閲覧前提のdocs等）は
  `[ルートからのパス](相対パス)`形式（例: `[docs/guide/setup.md](guide/setup.md)`）、
  HTMLに変換される想定（mkdocs・Sphinx等）は自然な表現で書く（例: `[セットアップ手順](guide/setup.md)`）
- 相対パスのリンク（`[text](相対パス)`）は配置先ディレクトリ基準で解決し、`test -e <解決後パス>`で存在を確認する
  - 検査対象は相対パスに限定し、`http`・`#`・`/`・`mailto:`で始まるリンクは対象外
  - コードブロック内の例示パスは対象外
- テーブルは列数が多い場合や内容が長い場合は箇条書きへの変換を検討する

## 技術文書の書き方

- 手順書（セットアップ・デプロイ・移行など）: 番号付きリストで1ステップ1動作
  前提条件・事前準備を冒頭に、成功時の期待結果を各ステップか末尾に、失敗時の対処・ロールバックも記載する
- トラブルシューティング: 症状 → 原因 → 対処の3要素で構成し、よくある問題から並べる
  エラーメッセージは検索しやすいよう正確に引用する
- API文書: エンドポイント・パラメーター・レスポンスを網羅し、具体例とエラーケースも記載する
- コミットメッセージ: 読者はエンドユーザー。内部リファクタリング等の詳細は不要。プロジェクト方針があれば従う

## ファイル構成

- ファイル粒度: 1ファイル1トピック。1ファイル300行超で分割を検討する
- ファイル命名: ケバブケース（`setup-guide.md`）。定番ファイルは大文字の慣例に従う
- コーディングエージェント向け文書（`CLAUDE.md`・`AGENTS.md`等）との役割分担: READMEは人間の読者向け
  重複情報はREADMEを正とし、コーディングエージェント向け文書からは参照するか要点のみ再掲する
