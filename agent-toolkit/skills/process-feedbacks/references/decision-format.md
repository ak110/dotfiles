# 採否結果と後始末

## 採否結果

要求ごとに次を記録する。素材単位の採否は記録せず、同じ素材から独立した要求行へ分解する。

```markdown
### <filename>: <要約>

- 素材ID: <事前割当した素材ID>
- 要求ID: <素材ID名前空間の要求ID>
- 素材参照: <要求を確定する素材IDの昇順リスト>
- 採否: 採用 | 不採用
- 理由: <実測した根拠。提案が前提とする仕組みを維持・修正する場合は、削除・簡素化案と比較した根拠>
- 投入元識別子: <frontmatterのsource文字列又は明示された出所。欄が無い場合は「値なし」>
- 引用範囲: <本文全文、全文、回答全文又は明示された範囲>
- 適用優先順位: <session-review起点の項目だけ。優先順位4に準じる判断を記載>
- 懐疑的判定の根拠: <session-review起点の項目だけ。通常起点より懐疑的に判定した根拠>
- 採用範囲: <採用時は実装範囲。不採用時は「非該当」>
- 除外範囲: <不採用時は除外範囲。採用時は「非該当」>
- 反映先: <採用時だけ>
- 反映内容: <採用時だけ>
```

要求案の文言、列挙又は節配置を技術判断で調整した場合は、差異と根拠を`理由`又は`反映内容`へ記録する。

投入元識別子は`source`の文字列又は「値なし」を改変せず記録する。
`session-review`は優先順位4へ照合する。
`alert-monitor`、`plan`、値なし及びその他の値は、値を保持したまま生成経路固有の規定へ照合する。
フィードバック本文又は投入元識別子から、人間由来又は利用者認可を推定しない。

独立した要求ごとに採否を確定する。
同一ファイルに採用要求が1件以上ある場合は、採用要求と不採用要求を要求表へ併記し、
不採用要求の理由と除外範囲を残す。不採用要求が利用者指示又は合意を覆す場合は、
`agent-toolkit/rules/01-agent.md`「協調と自律」節の確認経路へ送る。
`session-review`起点で既存対応済みと実測できる不採用は追加確認を要しない。

## ファイル単位の終端

- 採用要求が1件以上あり、未確定要求が無いファイルは、要求表へ採用・不採用の全要求を残して採用終端へ進める。
- 要求単位の部分採用では、不採用要求の採否理由と除外範囲を要求表へ残し、採用要求を根拠とする実施だけを計画へ含める。
- 全要求が不採用のファイルは、計画へ渡さず判定確定後にrejectする。
- 保留又はTBDなど未確定要求が1件以上あるファイルは、採用要求があっても計画へ渡さずholdする。
- `## 実施内容`の`根拠`は採用要求だけを参照し、不採用要求を実施の根拠にしない。

## 後始末

- 全要求不採用は判定確定後に`atk mq reject <filename> --note=<理由>`で終端させる
- 採用要求を含むファイルは対象commitのpush完了後に
  `atk mq adopt <filename> --note=<反映概要> --commit=<完全長SHA>`で終端させる
- 未確定要求を含むファイルは採否確定前に起草工程へ渡さず、確認経路又はTBDへ送る

`--note`には1行の結果を渡す。`--commit`は対象リポジトリの完全長SHAとし、rejectへは渡さない。
実行後に対象エントリを再取得し、`## 処理結果`の採否、note、commitを入力と照合する。
