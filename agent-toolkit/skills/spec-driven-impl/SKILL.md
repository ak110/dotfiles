---
name: spec-driven-impl
description: >
  `agent-toolkit:spec-driven-plan`で作成した計画ファイル群（`docs/v{next}/plans/*.md`）を
  全件巡回し、ユーザー不在の自律実行により順次実装する。
  各計画ファイルは`agent-toolkit:plan-impl`の手順で検証・コミット・レビューまで完遂する。
  確認事項は`docs/v{next}/TBD.md`へ書き残し続行する。
  ユーザーが回答を追記したうえで本スキルを再起動すれば回答済み事項を反映して続行できる。
# 編集時の注意点:
# 本スキル実行中は`agent.md`・`agent-toolkit:plan-mode`・`agent-toolkit:plan-impl`の
# ユーザー確認規範をオーバーライドする。対象は本文「2. ユーザー確認規範のオーバーライド」節で列挙する。
# 対象外（破壊的操作・公開IF変更等）は同節後段で明示する。
# `EnterPlanMode`/`ExitPlanMode`は使用しない（plan modeの編集制約と整合しないため）。
# 各計画ファイルの処理は`claude`サブエージェントへ委譲し、メイン側のコンテキスト消費を抑える。
---

# 自律実行による計画ファイル巡回実装

`agent-toolkit:spec-driven-plan`で作成した計画ファイル群を、ユーザーが応答できない前提で順次実装する。
確認すべき事項は`docs/v{next}/TBD.md`へ書き残し、暫定判断で続行する。
ユーザーが回答を追記したうえで本スキルを再起動すれば回答済み事項を反映して続行できる。

## 1. 位置付けと前提

入力前提:

- `docs/v{next}/plans/*.md`（計画ファイル群）が1件以上存在する
- 上記の配置は`agent-toolkit:spec-driven`配下`references/spec-driven-framework.md`の既定または
  プロジェクト指定に従う

起動方法:

- 引数なしで起動する（`/agent-toolkit:spec-driven-impl`または`agent-toolkit:spec-driven-plan`からの誘導）
- `docs/v{next}/plans/*.md`を全件巡回する
- 起動時に入力前提を満たさない場合の停止規定は「2. ユーザー確認規範のオーバーライド」末尾に従う

## 2. ユーザー確認規範のオーバーライド

本スキル実行中は次の確認契機をTBD.md追記＋暫定判断＋続行へ置換する。

- `agent.md`「品質最優先」節および「ユーザーとともに考える」節の確認要求全般
- `claude-code.md`「auto mode下での挙動」節「迷ったら作業を止めて確認」
- `agent-toolkit:plan-mode`の要件対話・認識合わせ・恒久化検討・リファクタリング検討の各ユーザー確認
- `agent-toolkit:plan-impl`「2.5サブエージェント完了報告の検収」のうちユーザー確認を要する分岐

オーバーライド対象外（本スキル中も従来規範どおりに停止する）:

- 公開インターフェース変更でユーザー合意が無いもの
- 破壊的操作・外部送信（`git reset --hard`・`rm`・`git push`・外部APIへの送信など）
- 検証コマンドの恒常的失敗（リトライで解消しない正当な失敗）

上記に該当する場合は当該計画ファイルの処理を打ち切り、TBD.md末尾に「## 停止理由」章を追加して
次の計画ファイルへ進む。全件処理し終えるまで本スキル自体は終了しない。

本スキル全体の実行前提が崩れる状態（`docs/v{next}/plans/`不在・走査結果が空など）を検知した場合は、
処理対象が成立しないため本スキル自体を即時終了する（次の計画ファイルが存在しないため）。
TBD.mdが存在すれば末尾に「## 停止理由」章を追加してから終了する。

## 3. 計画ファイル巡回ループ

1. `docs/v{next}/plans/*.md`の一覧を取得し、順に処理対象とする
2. `docs/v{next}/TBD.md`を読み込み、回答済み質問の反映方針を確定する
   - 未作成ならテンプレで新規作成する
3. 計画ファイル1件ごとに「4. 計画ファイル処理の委譲」を実施する
4. 全計画ファイルを処理し終えたら、TBD.mdの未回答件数と冒頭リストを最終メッセージで提示して終了する

複数計画を1コミットへまとめない（計画ファイル1件あたり最低1コミット）。

テーマ間の継続可否確認をユーザーへ問わず、未処理テーマがあれば全件を自動継続する。

## 4. 計画ファイル処理の委譲

メイン側のコンテキスト消費を抑えるため、計画ファイル1件あたり1サブエージェント（`claude`）を起動する。
当該サブエージェント内で`agent-toolkit:plan-impl`の工程を完遂する。
工程はタスク分解・実装・検証・コミット・レビューを含む。
メイン側は委譲・完了報告検収・次計画への遷移に専念する。

### サブエージェント起動プロンプト

```text
以下の計画ファイルを`agent-toolkit:plan-impl`スキルに従って実装してください。

- 計画ファイル: `docs/v{next}/plans/{theme}.md`
- 実施工程: タスク分解・実装・検証・コミット・レビュー
- 進捗は`plan-impl`規定どおり計画ファイルの`## 進捗ログ`へ逐次追記してください
- ユーザー確認が必要な場面は`agent-toolkit:spec-driven-impl`スキルのオーバーライド規範に従い、
  `docs/v{next}/TBD.md`へ質問ブロックを追記し暫定判断で続行してください
- オーバーライド対象外事象（公開IF変更・破壊的操作・恒常的検証失敗等）を検知した場合は
  当該計画の処理を打ち切り、停止理由を完了報告に含めてください
```

### 完了報告の検収

サブエージェント完了報告を検収し、次のいずれかへ分岐する。

- 正常完了報告: 次の計画ファイルへ進む
- 停止理由を伴う報告: TBD.md末尾に「## 停止理由」章を追記して次の計画ファイルへ進む
- TBD.mdに既存の回答済み質問があれば、暫定採用との一致／不一致を判定し、不一致なら次サイクル以降の
  改修タスクとして当該計画ファイルへ追記する

## 5. TBD.md書式

章構成: 冒頭に本ファイルの説明文、`## {theme name}`配下に`### Q{n}: {要約}`ブロックを並べる。
処理中に停止理由が発生した場合は末尾に`## 停止理由`章を追加する。

質問ブロック雛形:

````markdown
### Q{n}: {一行で要約}

- 発生工程: {計画|実装|検証|コミット}
- 関連箇所: `path/to/file` または `docs/v{next}/plans/{theme}.md`の節名
- 背景: {何が決められず何を仮置きしたか}
- 選択肢:
  - A: {...}
  - B: {...}
- 暫定採用: {ラベル}（理由: {根拠}）
- 回答:
  <!-- ユーザーはこの行以降に回答を追記する -->
````

回答欄判定: 直後のHTMLコメント行を除き非空行があれば「回答済み」と判定する。
非空行がなければ「未回答」と判定する。

## 6. 停止判定

「2. ユーザー確認規範のオーバーライド」のオーバーライド対象外事象を検知した場合、
当該計画ファイルの処理を打ち切り、TBD.md末尾に`## 停止理由`章を追加する。
本スキル自体は次の計画ファイルへ進み、全件処理し終えるまで終了しない。

`## 停止理由`章には次を記録する。

- 停止契機（公開IF変更・破壊的操作・検証失敗・前提崩れのいずれか）
- 対象計画ファイルパス
- 原因の要約
- 再開時にユーザーへ確認したい事項
