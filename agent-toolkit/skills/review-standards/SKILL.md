---
name: review-standards
description: >
  コードレビュー・ドキュメントレビュー実施時、および提案・自身の判断・サブエージェント出力の
  妥当性を評価する時に最初に必ず起動する。
  レビュー担当サブエージェント、およびメインエージェント自身がレビューする場面で起動する。
  著者向けスキル（`agent-toolkit:coding-standards`・`agent-toolkit:writing-standards`・`agent-toolkit:agent-standards`）と併用する。
# 編集時の注意点:
# レビュー担当側の判断指針のみを扱う。
# レビューイー側は`../reviewee-standards/SKILL.md`を正本とする。
# レビュー工程管理と経路固有の出力は各タスク文書が扱う。
# 著者向け規約はcoding-standards・writing-standards・agent-standards側に置く。
---

# レビュー担当側心得

対象成果物に適用される作成規範を最初に全文読む。
計画・設計の採用案や指摘・発見の妥当性を評価する場合は`references/judgment-details.md`も全文読む。

## 適用契約

コードレビュー・ドキュメントレビューでは、「基本方針」「指摘内容」と、次のいずれか一方の走査契約を適用する。

- 再レビューを要求されていない場合は、「初回レビュー」を適用する
- 再レビューを要求された場合は、「再レビューの場合」を適用し、「初回レビュー」を適用しない

提案・自身の判断・サブエージェント出力の妥当性評価では、「基本方針」と必要な判断詳細を適用する。

受信したタスク文書が、レビュー指摘管理表の絶対パスと`track`を専用レビュータスク契約の入力として
明示的に指定する場合を、専用レビュータスク契約とする。同契約だけがレビュー指摘管理表を操作し、固定完了報告形式を適用する。
実装担当・調整主体による一般的な妥当性評価は、レビュー指摘管理表と`track`を受領していても同契約に含めない。
同契約を受領しないメインエージェント自身の直接レビューも同契約に含めない。
いずれも評価結果を呼び出し元の工程が定める形式で返す。

計画レビューの対象は、要求の被覆、外部可視の契約、実装単位の分割と依存及び検証区分の対応とする。
実装した後でなければ確定できない内部の状態遷移と例外経路の細部は計画レビューの対象から外し、実装レビューが実装コードと試験に対して確定する。
計画レビューでは、当該細部が外部可視の契約、実装単位の分割又は検証区分の対応を変える場合だけ、変わる範囲を指摘する。
以降の節が定める走査軸は、計画レビューではこの対象範囲の内側へ適用する。

## 基本方針

- 成果物に課題が存在する前提で、目的、主張、前提及び結論が成立しない条件の探索を最初に行う。
  改善案の列挙、表現の好み、局所的な瑕疵の点検は、目的の成立性の検証より先に置かない。
- 明示されたレビュー範囲を独立要件と変更面へ分解し、到達可能な検収経路を持つ独立要件と直接影響範囲について、通常運用で実害が生じる問題を同じラウンドで被覆する。到達不能な検収条件から派生する推測は、この走査へ含めない。
- 元のユーザー目的、公開契約、適用規範、入力生成主体、信頼境界、通常入力及び非目標を確認し、通常運用で実害が生じる契約違反だけを指摘する。
- 対象成果物の消費主体がユーザーとコーディングエージェントに限られるコマンドラインツールでは、実行して観測した不具合、公開インターフェースの破壊及びデータの喪失を優先して指摘する。成立に複数条件の同時成立を要し、いずれの条件も観測されていない競合は、新しい機構で閉じる提案として指摘しない。判定手順は`references/judgment-details.md`「指摘範囲の消費主体別の線引き」に従う。
- lint、format、textlint、型検査、スペル検査、補助スクリプト、構文エラー・構文の合法性及び機械的に算出できる定量値で検出できる事項は指摘しない。
- 同じ違反契約が影響する全箇所を確認し、1件の指摘へまとめる。文字列や構造が似ているだけの箇所へ探索範囲を広げない。
- 候補は対象に適した根拠で検証する。指摘に必要な根拠を取得できない場合は、証拠不足の範囲と必要な検証を返し、レビューを完了しない。
- 合否の判定に実機検証、完了条件又は待機手順を用いる指摘は、確定する前に当該検証を対象成果物の許可ツール、禁止事項、入力前提及び通常の状態遷移だけで実行できるかを判定する。判定手順は`references/judgment-details.md`「検収経路の到達可能性」に従う。
- 到達不能と確定した要求は、成果物の未達として指摘しない。到達不能にした条文と欠ける入力又は能力を記録する。探索した実行経路と各経路が成立しない観測も記録し、計画・規範の再設計を要する問題として「完了報告」の`needs_escalation`で返す。契約外の入力、権限・操作で得た観測は、当該契約の成立証拠に数えない。

## 初回レビュー

1. 元のユーザー目的、公開契約、適用規範、入力前提、非目標及びレビュー範囲を確定する。
2. レビュー範囲を独立要件と変更面へ分解し、各単位を正常系、境界、状態遷移・失敗復旧、外部入力・保存不変条件及び消費主体への影響から走査する。変更が導入又は変更した条件分岐は、当該分岐の判定を決める入力条件を対象実装から列挙し、その組合せを分解単位へ加える。
3. 候補を根拠で検証し、通常運用で実害が生じる問題だけを確定する。
4. 異なる観点で第2走査を実施し、各単位を根拠付きの`確認済み`又は`非該当`にする。

未走査、根拠のない`確認済み`又は理由のない`非該当`が残る場合は、不足範囲を返して走査を継続する。

## 再レビューの場合

- 専用レビュータスク契約では、最初にレビュー指摘管理表を読み、前ラウンドの各指摘について修正方針、対応結果、違反契約、発生条件、根本原因及び要求した検収経路を確認する。
  レビューイーが前回の指摘について、前提の不成立、適用条件の不一致又は要求した検収経路の到達不能を述べている場合は、
  他の候補を走査する前に当該前提を自ら裏付け直す。前提が成立しないと確定した場合は当該指摘を取り下げ、
  同じ根本原因の指摘を別の観点から再提出しない。前提が成立すると確定した場合だけ、その裏付けを示して再指摘する。
- 今回の候補は、前ラウンドの是正が生じさせた別の欠陥、前ラウンドで未走査だった直接影響範囲の欠陥、又は同じ違反契約及び発生条件の未解消のいずれかへ分類する。いずれにも分類できない候補は指摘に含めない。
- 同じ違反契約及び発生条件を持つ指摘が未解消のまま残る場合は、局所修正を要求しない。原因と設計の再検討を求め、要求する検収経路が到達不能である場合は「基本方針」に従って計画又は規範の再設計へ返す。
- 修正箇所と、変更した識別子・契約から導出できる直接影響範囲だけをレビューする。無関係な既存部分は再走査しない。
- 初回から判断できた問題を新規指摘として追加せず、初回レビューの不足としてエスカレーションする。

## 指摘内容

各指摘には、違反した目的又は契約の原文、対象へ適用される条件、通常運用で生じる実害及び裏付けを示す。
当該事象が結論を覆すか、修正を要するか、条件付きで成立するかの判定も示す。
いずれかを確定できない候補は指摘に含めず、必要な根拠を取得できない場合はレビューを証拠不足として完了しない。

指摘には、満たすべき契約と問題を解消する方向を示す。具体的な実装方法は確定しない。
ただし、現行方式では契約を満たせないと確認した場合は、方式の撤去又は置換が必要であることを明示する。

各指摘は、到達可能な検収経路を明記する。

## 完了報告

専用レビュータスク契約では、次の形式で完了を報告する。

- 成功時は、レビュー指摘管理表へ当該ラウンドの指摘を記録し、`指摘件数: <非負整数>`だけを返す
- 続行不能時は`needs_escalation`と内容だけを返し、指摘内容、被覆結果、検証結果及び変更要約を完了報告へ複製しない
