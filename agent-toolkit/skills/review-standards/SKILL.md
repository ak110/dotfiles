---
name: review-standards
description: >
  コードレビュー・ドキュメントレビュー実施時に最初に必ず起動する。
  レビュー担当サブエージェント、およびメインエージェント自身がレビューする場面で起動する。
  著者向けスキル（`coding-standards`・`writing-standards`・`agent-standards`）と併用する。
# 編集時の注意点:
# レビュアー側の判断指針のみを扱う。
# レビュー工程管理はplan-impl-executorと実装差分レビューの共有referenceが扱う。
# 著者向け規約はcoding-standards・writing-standards・agent-standards側に置く。
# 機械チェックが扱う項目は本スキルでも非対象とする方針のため、
# 検出語そのものを本文へ転記してはならない（コンテキスト汚染を招く）。
# 「計画ファイル文脈での例外」節は機械判定可能な体裁項目のSSOTとする。
---

# レビュアー側心得

レビュー出力は次の完成条件をすべて満たす。

## 完成条件

- 指摘候補は内部的に網羅列挙してから確実な項目のみへ限定する。
  同じ混入・動機・見逃し構造から不具合が生じる箇所を確認し、表面の実装や抽象化階層が異なる箇所も含める
- 機械チェック（lint・format・textlint・型検査・スペル検査・補助スクリプト）で判定できる項目は扱わない。
  行数など定量項目もレビュアーが推定算出して指摘しない
- 各指摘は対象差分・周辺コード・参照仕様を確認した確実な誤り・規約違反・欠陥のみとし、
  「調査が必要」「確認した方がよい」等の曖昧な留保のまま報告しない。
  現コードで発生しない仮定ベースの懸念、プロジェクト方針に根拠のない主観的好みは報告しない
- 確実な指摘は重大度を問わず全件報告する。重大度ラベルは`致命的`（要件未達・データ破損・
  セキュリティ欠陥）・`重大`（設計欠陥・整合性の崩れ・テスト不足）・`軽微`（スタイル・改善余地）の
  3種のみを用い、いずれにも自己分類できない指摘は追加調査で確定するか取り下げる
- 既存規範への違反として指摘する場合は当該規範の出典（ファイル・節・原文）を引用形式で明示する。
  引用できない事項は規範違反として指摘せず改善提案として別扱いとする
- 構文の合法性はlint・型検査の担当領域のため指摘対象外とする。対象言語バージョンで有効化された
  新構文を学習知識との齟齬で構文エラーと判定しない。判定前に実行環境バージョン
  （`.python-version`・`requires-python`・`engines`等）を確認する
- 指摘は行頭に`[重大度] path:123`形式の箇所指定を付け、修正方針を簡潔に併記する。
  機械チェック辞書の検出語・違反パターンを再現する具体文字列は指摘文へ転記せず観点を抽象的に説明する
- 指摘が無い場合は本文を`指摘なし`のみとし前置き・確認過程・全体サマリーを含めない。
  最終指摘リストは1回のみ出力し、試行錯誤の中間版を含めない
- gitignore対象ファイル（`CLAUDE.local.md`・`.env`等）を根拠とする指摘は本文表現を
  抽象化した観点で書き、非公開内容を統合先の成果物へ転記しない

## 計画ファイル文脈での例外

計画ファイル本文（`~/.claude/plans/*.md`）は廃棄される一時作業文書であり、次を指摘対象外とする。

- 記述スタイル（章構成・段落構成・表現選択・書き方）への指摘。記述間の矛盾は対象に含む
- 実装時にエージェントが判断可能な細部（変数名・エラーメッセージ文言・局所的な制御フロー等）
- 件数表現・行番号・行数・ファイルサイズなど機械検査で判定できる項目

計画レビューの指摘範囲は次のとおりとする。

- 指摘対象は、計画本文の矛盾、計画した変更が生じさせる欠陥、
  または計画を実施不能にする既存契約との不整合とする
- 類似箇所は、同じ計画変更が適用できるかではなく、同じ混入・動機・見逃し構造から
  不具合が生じるかで判定する。表面の実装または必要な修正が異なっても、同じ失敗構造なら指摘へ含める
- 初版の成立性と独立する既存問題は計画レビューと分ける。
  同じ失敗構造に属する問題は、初版の類似見直し範囲との整合を確認して区分する
