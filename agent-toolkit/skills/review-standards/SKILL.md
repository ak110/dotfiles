---
name: review-standards
description: >
  コードレビュー・ドキュメントレビュー実施時のレビュアー側心得。
  些末な指摘の排除、確信度の扱い、機械チェックとの役割分担、指摘の根拠形式を含む。
  レビューを担当するサブエージェント、およびメインエージェント自身がレビューする場面で最初に必ず呼び出すこと。
  対象がコードかドキュメントかを問わず適用する。
  成果物を読んで理解する場面、機械チェック単独実行の場面ではトリガー不要。
# 編集時の注意点:
# レビュアー側の判断指針のみを扱う。
# レビュー工程管理（起動・分担・指摘統合・再レビュー）はcareful-reviewスキルが扱う。
# 著者向け規約はcoding-standards・writing-standards・agent-standards側に置く。
# 機械チェックが扱う項目は本スキルでも非対象とする方針のため、
# 検出語そのものを本文へ転記してはならない（コンテキスト汚染を招く）。
---

# レビュアー側心得

## レビューの進め方

レビュー実施時は次の順序で進める。

1. 考えられる指摘候補を内部的に網羅列挙する
2. 曖昧な指摘候補は追加調査をして明確化する
3. 確実に問題である致命的かつ本質的な欠陥のみを残し最終応答とする

## レビューの基本姿勢

- 機械チェック（lint・format・textlint・型検査・スペル検査など）で判定できる項目は扱わない
  - 代表的な対象は行幅・文字数・空行・口語表現・型違反・未使用変数・lint違反・format違反・スペル違反など、
    検査コマンドの実行結果として一意に判定できる事項である
  - 補助スクリプト（`scripts/check_line_width.py`・`scripts/check_colloquial.py`等を含む）の実行もレビュー時には行わない
- 確信度の低い指摘は記載しない。
  対象差分・周辺コード・参照仕様を確認したうえで、確実な誤り・規約違反・ロジック欠陥と判断できる項目のみ指摘する
- nice-to-have・主観的提案・スタイル懸念、および主観的好み（命名好み・並び順好み・粒度好みなど、
  プロジェクト方針やルールに明示されない選好）は指摘対象外とする
- 既知の事前存在問題、明示された修正対象外の領域、計画範囲外で別途対応する旨が示された箇所は指摘対象外とする
- 対象範囲の指摘をすべて列挙する。修正後の再レビューも新規エージェントとして起動し、毎回対象範囲を独立に評価する
- 実装担当者の報告は参考情報として扱い、実装事実はコードと差分で確認する
- 指摘を途中で打ち切らず、初回のレビューですべての問題点を洗い出す

## 指摘の表現

- 指摘は必ず`path:123`形式の箇所指定を含める
  - 対象が複数行にわたる場合は代表行または範囲のいずれかを示す
- 指摘には修正方針を簡潔に併記する
- 推測で断定せず、確認した事実に基づいて記述する
  - 確認できない事項は指摘せず、必要なら確認手段を取った上で再評価する
- 機械チェック辞書（口語表現辞書・spell辞書など）の検出語、および違反パターンを再現する具体文字列を指摘文へ転記しない
  - 該当する観点を抽象的に説明する形で記述する（指摘文経由で対象コンテキストへ違反パターンが混入する事態を避けるため）
  - コンテキスト汚染と無関係な悪い例（ロジック上の不備例・設計判断の誤り例など）は通常の指摘文として記述してよい
- 指摘が無い場合は本文を`指摘なし`だけにし、前置き・確認過程・全体サマリーを含めない
