---
name: delegation
description: >
  別の実行主体へ作業を委譲するときに起動し、送信・継続・受領の共通契約を提供する。
user-invocable: false
---

# 委譲

送信側の共通契約を定める。受信者が行う用途固有の作業は、受信者用task referenceまたはagent定義を正本とする。

## 送信

1. 委譲する独立した作業、読取専用か書込可能か、完了条件、受領経路を確定する
2. 専用agentまたはtask referenceがある場合はそれを選び、無い場合だけ汎用委譲先を使う
3. 実行環境、model、worktree、snapshotの経路固有判断が必要な場合だけ、
   起動直前に`references/runtime-routing.md`を全文読む
4. 起動文の先頭で受信者への命令を1文で示す。続けて次の値だけを渡す
   - task reference、成果物、プロジェクト規範、必要なauthor skillの絶対パス
   - 作業ディレクトリ、対象、読取・書込権限、不可逆操作権限、完了条件
   - 実行時に確定した値、今回限りの例外、追加指示
5. task referenceの手順、品質規範本文、出力schema、過去応答を起動文へ複製しない

作業ディレクトリは受領済みの絶対パスをそのまま渡す。受信者に自己解決させない。
作業用複製では、複製元と対象外worktreeも明示する。

## 継続と新規起動

- 同じ作業と判断履歴を継続する場合は、同じrouteと識別子へ未完了事項、新しい事実、参照pathだけを送る
- 独立したreview系統、異なる観点、先行履歴に依存しない評価は新しい識別子で起動する
- 同じ識別子への追送は先行応答の受領後に行う。稼働中は方針を逐次差し替えず、確定した差分を1回で送る
- 中断済み、完了配送不能、前提が無効化された識別子は再利用せず、検収済み状態を渡して新規起動する

## 受領と検収

通常のツール戻り値または完了通知を第一の受領経路とする。
受領後は、報告本文より先に成果物、Git状態、実行識別子、検証結果を実測する。
task referenceの必須欄、対象、権限、完了条件と照合し、次のいずれかだけを返す。

```text
route: <実際に使った経路>
identifier: <threadまたはagent識別子>
status: completed | needs_escalation
response: <受信者の最小完了報告>
```

待機表明だけの応答、必須結果不足、成果物との不一致は完了と扱わない。
記録経路は通常配送不能を実測した場合だけ使用し、所有主体の終端と内容検収後に管理対象一時領域を後始末する。
