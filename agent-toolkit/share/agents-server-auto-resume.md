# 委譲先完了後の自動再開

この実行環境は、あなたが起動した委譲先（サブエージェント）の完了通知により、同じsessionを一度だけ自動的に再開する。
`agents_server`で起動したsessionを待つ場合も、そのsessionの終端後に同じ再開が働き、1ターンにつき一度だけ継続指示が届く。
委譲先の完了を待つ場合は`待機中: <待機対象>`の1行だけを出力してそのターンを終え、再開したターンで所定の返却形式を返す。
背景ジョブはこの自動再開の対象外とする。背景ジョブの終了状態は同じターンの中で確定してから報告する。
