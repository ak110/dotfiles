---
# 同期注記: 「実装委譲（plan-codex-delegate / plan-implementer）の判断指針」節手順5の並列化条件
# （呼び出し元起動プロンプトでの明示指定を前提とする、実装委譲固有の制約）は、
# `agent-toolkit/agents/plan-impl-executor.md`「停止禁止」節、
# `agent-toolkit/agents/spec-driven-implementer.md`「前提」節「停止禁止」バレットと意図的に重複させている。
# 文面を変更する場合は3ファイルの整合を取ること。
---

# plan-impl-executor 工程1〜5の実体手順

`agent-toolkit/agents/plan-impl-executor.md`から起動時にReadで読み込まれるSSOT。
渡された計画ファイル1件を工程1〜5（タスク分解・実装・検収・検証・コミット・レビュー）まで完遂する手順を定める。

レビューは工程5「レビュー実施」で扱う。

`## 変更内容`に列挙された全項目を初回実装で完遂する（「初回必須」「将来拡張」など段階化ラベルを付与した先送りは禁止する）。

`ExitPlanMode`によるユーザー承認は、計画ファイル全文の精読を保証しない。
ユーザーは`## 背景`・`## 対応方針`を中心に確認したうえで承認している場合が多く、
`## 変更内容`・`### エージェント判断`の細部や周辺対応までは未読の可能性がある。
実装段階で計画ファイル本文が判断指針と衝突する場合の扱いは同節に従う。
判断指針は`01-agent.md`「コーディングエージェント（あなた）の振る舞い」節の「品質最優先」、
および`agent-toolkit/rules/02-collaboration.md`の「協調モード」節を指す。
plan-impl-executor固有の差分として、ここでの衝突は計画ファイル本文の明示記述と判断指針の明示記述が
文面上矛盾する場合に限る。自己の不安・疑念・主観的な違和感・自己推定上の制約は衝突に該当しない。
衝突を発見した時点で実装を止め、計画ファイル本文優先の暫定判断で続行し、
`agent-toolkit/rules/02-collaboration.md`の「自律実行モード」節「3. TBD記録の書式」に従い記録する。
確認事項は完了報告の`pending_confirmations`欄へ集約し、呼び出し元メインが検収後にユーザーへ確認する。

実行中にユーザーから新たな割り込み指摘・是正要求が発生した場合の扱いは、
`agent-toolkit/rules/02-collaboration.md`の「協調モード」節に従う。
同節は割り込み指摘の即時報告・恒久化検討の並行実施、工程4「恒久化検討」の即時実施を定める。
固有の差分として、恒久化候補の確定文面は計画ファイルの`### 恒久化・リファクタリング内容`へ追記する。
割り込み指摘自体を「次セッションへの先送り」「`session-review`・`feedback`への先送り」として残置しない。

`ExitPlanMode`はユーザー承認応答の取得まで含めてワンステップで完結する。
承認応答を受領した時点で実装着手の合意が成立しているため、
「実装に進めてよいか」等の追加確認文を発さず、直ちに以下の工程1へ着手する。

以下の手順は省略不可の工程である。
列挙外の場面でも`01-agent.md`「品質最優先」原則に従う。

1. タスク分解
2. 実装
2.5. サブエージェント完了報告の検収
3. 検証
4. コミット
5. レビュー（レビューステップでレビュー実施が指定されている場合のみ。記載のスキル・エージェントを起動し指摘反映まで完了する）

各フェーズの区切りの節目（1タスク完了・1コミット作成直前・レビュー指摘反映の節目など）ごとに、
計画ファイルの`## 進捗ログ`章へ`### <ラベル>`見出しと箇条書きで追記する。
書式詳細は`agent-toolkit:plan-mode`スキル配下の`references/plan-file-guidelines.md`の「進捗ログ」節に従う。
いずれのフェーズでも、計画ファイル本文（`## 変更内容`・`## 対応方針`等）の記載と乖離する確定形を採用した場合は、
`## 進捗ログ`への追記だけでなく当該本文を最終確定形へ更新する。
適用対象にはレビュー指摘対応によるdiffコードブロック内文面案の意味的変更も含む。
また`scratchpad`で改訂後最終形を構築した結果が計画文の
`### エージェント判断`記載のトリム内訳・抽出方針と乖離する場合も含み、
`scratchpad`完成時点（本ファイル反映前）で計画文を更新する。
`## 変更内容`配下の対象ファイル一覧の追加・除外・改名
（既存違反の同時修正・周辺対応の事後組み込み等）も乖離扱いに含み、
対象ファイル一覧のチェックボックス項目と各ファイル変更方針の`###`見出しを同時に追従して更新する。

実装着手前に計画ファイル本文の段取り記述（`## 対応方針`配下の`### 改訂作業の段取り`等）を読み返し、
「記録する」「反映する」等の進捗ログ追記要件を抽出してフェーズ末の記録対象に組み込む。
計画ファイル本文側で明示した個別の追記要件は、上記のフェーズ節目記録と並行して履行する。

## 1. タスク分解

タスク分解に着手する直前に本エージェント自身で`git rev-parse HEAD`を実行し、結果を計画着手前SHAとして記録する（工程5「レビュー実施」の不変参照点に使う）。
計画ファイルを起点にタスク分解し、`TaskCreate`で登録する（検証・コミット・レビュータスクも含む）。
過度な細分化は避ける。ファイル削除（`rm`等）はサブエージェントへ委譲しない。
レビュータスクは観点ごとに別サブエージェントを起動する（実装の経緯を持ち込まないため）。

計画`## 変更内容`の対象ファイル数が5以上の場合、タスク分解に先立って計画着手前SHAから現HEADまでの
`git log --oneline <計画着手前SHA>..HEAD -- <path>`を対象ファイルごとに実行する。
並列セッション由来の重複コミット有無を確認する。
重複を検出した場合は当該項目を計画対象から除外するか、既存対応と統合する形へ設計変更する。

## 2. 実装

分解したタスクを下記「実装委譲（plan-codex-delegate / plan-implementer）の判断指針」に従って実装する。

### 実装委譲（plan-codex-delegate / plan-implementer）の判断指針

計画確定後の実装はcodex委譲（`plan-codex-delegate`）を最優先とし、
codex利用不可の場合のみ`plan-implementer`委譲を選ぶ。
codex委譲は能力的にOpus相当扱いとし、コード・テストコード・一般ドキュメント・
コーディングエージェント向け文書（`agent-toolkit/`配下・規範文書等を含む）の
全種別の実装で最優先の委譲先とする。
メイン直接実装は小規模な修正（1行未満のtypo修正等）のみを対象とする。
委譲可否は自己推定上のタスク規模・対象範囲・所要量で判断せず、
タスク内容の確定度（修正方針・対象ファイル・期待結果の明確さ）で判断する。
タスクごとに次の手順で委譲先を決める。

1. タスクが小規模な修正（1行未満のtypo修正等）に該当するか見積もる
2. 小規模な修正のみメイン直接実装を選ぶ
3. Agentツールで`plan-codex-delegate`サブエージェント（`用途: 実装`）を起動する。
   対象ファイルが呼び出し元プロジェクト定義の編集用スキル起動義務（`agent-toolkit-edit`等）の
   対象範囲に該当する場合は、起動プロンプトの`## 遵守事項`節へ当該スキル呼び出し義務を明記して
   品質ゲートを履行する。判定基準は呼び出し元プロジェクトの規定に従う。
   `plan-codex-delegate`はMCP不可時に自身でCLIへフォールバックするため、
   `plan-implementer`への委譲はMCP・CLIいずれも利用不可（`plan-codex-delegate`の完了報告で判明する）な
   場合のみとする。`plan-implementer`へ委譲する場合の`model`引数は`sonnet`を既定とし、
   難易度に応じて`haiku`または`opus`を明示指定する
4. 委譲したタスクが`needs_escalation`を返したら本エージェント自身で引き上げて実装する。
   `plan-codex-delegate`（`用途: 実装`）は`status`/`summary`/`thread_id`/`changed`/`unplanned`の
   構造化書式で完了報告を返すため、`unplanned`欄で対象外変更の必要性などを検知し、
   該当時は自身での実装または`plan-implementer`へ切り替える
5. 呼び出し元起動プロンプトで並列化が明示指定された場合に限り、
   委譲タスクのうち編集対象ファイルが独立する2件以上は並列起動する。
   `plan-implementer`委譲・codex委譲のいずれも並列対象とする。
   `plan-implementer`委譲は`run_in_background=true`で並列起動する。
   並列化が明示指定されない場合は`plan-implementer`委譲を`run_in_background=false`で直列実行する。
   codex委譲の並列実行手順・formatter/linterの実行タイミング・検収方式は
   `agent-toolkit/agents/plan-codex-delegate.md`本文を正典とする
6. codex委譲タスクの`threadId`はplan-codex-delegateサブエージェントの完了報告（`thread_id`欄）で返却される。
   記録先・継続呼び出し時の参照は`agent-toolkit/agents/plan-codex-delegate.md`
   「報告」節を正典とする

- 呼び出し元起動プロンプトが「実装委譲（plan-codex-delegate / plan-implementer）の判断指針」節の
  委譲先選定と衝突する形で委譲先を明示指定するケースを扱う。
  このとき「実装委譲（plan-codex-delegate / plan-implementer）の判断指針」節の規範を優先して委譲先を確定する。
  呼び出し元指示は文書種別・タスク内容判定への追加情報として扱う
- 呼び出し元指示と「実装委譲（plan-codex-delegate / plan-implementer）の判断指針」節の規範との衝突が観測された場合を扱う。
  `plan-impl-executor`は既存の進捗ログ書式に従い当該衝突と選定結果を1バレットで記録してから実装へ進む

タスク内容の確定度に迷うタスクは手順3に従ってまず委譲する。
検証タスクを委譲する場合は、対象検証コマンドを起動プロンプトの`## タスク`欄へ明記する。

### サブエージェント起動プロンプト

起動プロンプトの雛形は`agent-toolkit/references/plan-impl/launch-prompts-drafting.md`
「起草・改訂委譲雛形」節を機械転記して構築する。
埋め込み欄は計画ファイルパス・文書種別・担当範囲区分・タスク記述・修正指摘（修正再実装時のみ）・完了報告要件。
運用ガイダンス（`[乖離]`サブバレット報告確認・計画本文との整合維持・雛形本文の実在確認等）は
`launch-prompts-drafting.md`側の「運用ガイダンス」節へ集約する。

## 2.5 サブエージェント完了報告の検収

サブエージェント完了報告を受領した時点で、次のレビューまで保留せず即時に検収する。
codex委譲応答も本節の検収対象に含み、unplanned相当の計画外事項はcodex応答本文の内容判断で抽出する。
unplanned項目が含まれる場合は自身で採否判定する。
unplanned項目とは`[CLAUDE.md追記候補]`・`[即時相談候補]`・`[該当外]`のいずれかのラベルを持つ計画外の発生事項を指す。

- 完了報告のchanged一覧が計画ファイル`## 変更内容`当該節の独立変更項目（複数行・複数識別子の更新指示等）を網羅するかを照合する
  - 1ファイル内に複数箇所の更新指示がある場合は一部のみ更新して報告するケースがあるため`git diff`で差分実体を確認する
  - 計画ファイル`## 変更内容`の対象ファイル節から個別指示項目
   （テスト追加・`docstring`更新・`assert`追加・命名変更等）を抽出してチェックリスト化する
    - 各項目について実装差分（`git diff`実体）に該当変更が含まれることを1対1で照合する
    - 完了報告の`changed`欄が概念レベル（「既存テストのリネーム」等）で集約され
      項目抽出が困難な場合は、計画ファイル指示と実装差分を直接照合する代替経路を採用する
- 完了報告の`changed`欄で、起動プロンプト`## 完了報告要件`に従い各タスク項目の個別指示と対応箇所が
  1件につき1サブバレットの形で展開されているかを照合する
- unplanned項目が含まれる場合は計画方針および判断指針へ照らして即時採否判定する
- 採否を保留すべき事項は暫定不採用（計画に明記のない変更は適用しない）でTBD相当記録し、
  完了報告の`pending_confirmations`欄へ記録する
- 修正再実装の検収では、修正差分（`git diff`実体）が新規問題の混入を生じていないかも事後点検する。
  観点は`agent-toolkit:careful-review`「指摘の統合と修正依頼」節の修正案の採用前検証と同一とし、
  当該節を典拠とする
- 完了報告で計画本文と異なる担当ファイル・識別子・命名規約が採用されたことを検出した場合、
  unplanned項目採否判定と同一タイミングで、計画本文を同期更新する
  - 同期対象は`## 変更履歴`・`## 変更内容`・`### エージェント判断`
  - 同期更新は本エージェントの責務とし、後続レビュー工程へ持ち越さない

## 3. 検証

全実装タスク完了後に検証する（実装タスクへの統合も可）。
検証コマンドは計画ファイルの`## 実行方法`の検証ステップの対象範囲全体。

実装完了後、`uv run --script agent-toolkit/skills/plan-mode/scripts/check_plan_file.py <計画ファイルパス>`を実行する。
`agent-toolkit:agent-standards`「文書サイズ上限」節の対象文書が対象ファイル一覧に含まれる場合を扱う。
実装後は対象ファイル本体の220行ハードリミットのみを検証する（`check_doc_size.py`と`wc -l`実測で判定する）。
計画本文の`（現行N行）`表記は変更前の基準値のため更新しない。

## 4. コミット

計画ファイルの`## 実行方法`のコミットステップに従ってコミットする。

## 5. レビュー実施

計画ファイルの`## 実行方法`のレビューステップを読み取り、記載のスキル・エージェント（複数併記時は全件）を本エージェントが呼び出す。
スキル（`agent-toolkit:careful-review`等）はSkillツール、エージェント定義はAgentツールで起動する。
起動時は対象範囲（全コミット完了後の差分）・計画ファイルパス・不変参照点（本エージェントが工程1着手直前に`git rev-parse HEAD`で自己記録した計画着手前SHA）・一時ファイル一覧を引き渡す。
指摘統合・修正再実装・再レビューは`agent-toolkit:careful-review`スキル本文の手順に従う。
対象はレビュー系サブエージェント（`plan-spec-reviewer`・`plan-impl-reviewer`・`agent-doc-validator`等）とする。
`run_in_background=false`のforeground並列起動（Agentツールを単一メッセージ内で複数回呼び出し）に統一する。
本エージェント自身のターンが全レビュアー完了まで維持されるため、`background`起動は選ばない。
最終的な指摘の反映完了状態を完了報告の`review_handoff`欄へ「実施完了（採用指摘N件反映）」または「レビューは実施しない」の形式で記録する。

完了報告の受領後の呼び出し元向け手順は`agent-toolkit/references/plan-impl/caller-reception.md`を参照する。
