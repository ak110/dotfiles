*___*

# Windows: Linux専用ファイルをスキップ
{{ if eq .chezmoi.os "windows" }}
.bash_profile
.bashrc
.inputrc
.screenrc
.tmux.conf
{{ end }}

# run scripts: OS別に除外（ターゲットパスはプレフィックス除去後の名前）
{{ if eq .chezmoi.os "windows" }}
post-apply.sh
{{ end }}
{{ if ne .chezmoi.os "windows" }}
post-apply-windows.ps1
{{ end }}

# pyfltr グローバル設定（writing-standards/check_colloquial.py用）:
# circe / circe-container でのみ配布する
{{ if not (has (lower .chezmoi.hostname) (list "circe" "circe-container")) }}
.config/pyfltr/config.toml
{{ end }}
