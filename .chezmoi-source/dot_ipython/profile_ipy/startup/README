このディレクトリはIPythonのstartupディレクトリです。

このプロファイルを読み込むと、`exec_lines`や`exec_files`で指定した処理より前に、
このディレクトリ内の`.py`ファイルと`.ipy`ファイルが実行されます。

ファイルは辞書順で実行されるため、以下のような接頭辞で実行順を制御できます。

    00-first.py
    50-middle.py
    99-last.ipy
