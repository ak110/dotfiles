# pyproject.toml hash: {{ include "pyproject.toml" | sha256sum }}
# chezmoi apply を止めないよう、失敗時は警告のみ
try {
    uv tool install --force --editable $HOME\dotfiles
    uv tool update-shell
} catch {
    Write-Warning "pytools install failed: $_"
}
