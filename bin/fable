#!/usr/bin/env bash
# NOTE: 対応する Windows 版 → bin/fable.cmd
source "$(dirname "$0")/_claude-wrapper.sh"
run_claude --permission-mode=auto --model=fable --fallback-model='claude-opus-4-7[1m]' --effort=low "$@"
