# テンプレート集

spec-drivenスキルで使う恒久ドキュメント（機能・横断・`README.md`）・一時ドキュメント・参照コメントの書式テンプレート。
参照コメントの配置方針などはSKILL.md本体を参照する（本ファイルは書式のみ提示する）。

## 機能ドキュメント（`{機能名}.md`）

実装後も残し、将来の類似改修時に参照される。
コードを見ただけでは分からない情報（なぜその設計を採ったか・却下した代替案）を中心に書く。

````markdown
# {機能名}

## 背景

（なぜこの機能を追加するか。ビジネス要求・ユーザー要望・既存の課題など）

## スコープ

（本機能で扱う範囲と扱わない範囲）

### 含むもの

-
-

### 含まないもの

-
-

## 受け入れ基準

（機能が完成したと判断できる客観的な基準）

-
-

## 主要設計判断

（設計で確定した主要な方針と理由。将来の類似改修時に参照される核心情報）

### {判断項目1}

- 決定内容:
- 理由:

### {判断項目2}

- 決定内容:
- 理由:

## 却下した代替案

（検討したが採用しなかった代替案と却下理由。同じ議論の再発を防ぐため）

- 代替案A — 却下理由:
- 代替案B — 却下理由:
````

## 横断ドキュメント（`{トピック名}.md`）

複数機能で参照される設計判断・ガイドライン・共通用語をまとめる。
機能ドキュメントのテンプレートを流用し、以下の差分だけ意識する。

- 受け入れ基準は機能単位でないため省略してよい
- 「スコープ」は対象範囲（どの機能から参照されるか・どういう場面で参照するか）に読み替える
- 「背景」には横断化した経緯（どの機能間で共通と判断したか）を書くと再利用しやすい

## バージョン概要（`README.md`）

`docs/v{version}/README.md`に置くバージョン全体の入口。
ステータス列は含めない（進行状況の管理は`.working.md`・TaskCreateに委ね、恒久ドキュメントに陳腐化する情報を載せないため）。

````markdown
# v{version}

## バージョン目的

（このバージョンで実現したい目的・テーマ）

## 機能一覧

（本バージョンで追加・変更する機能。各機能ドキュメントへのリンクと一行説明）

- [docs/v{version}/{機能名1}.md]({機能名1}.md) — 一行説明
- [docs/v{version}/{機能名2}.md]({機能名2}.md) — 一行説明

## 横断ドキュメント一覧

（複数機能で参照される設計判断・ガイドライン・共通用語）

- [docs/v{version}/{トピック名1}.md]({トピック名1}.md) — 一行説明
- [docs/v{version}/{トピック名2}.md]({トピック名2}.md) — 一行説明
````

## 一時ドキュメント（`{機能名}.working.md`）

実装完了時に削除する作業メモ。
恒久情報は`{機能名}.md`に転記した後、このファイルを削除する。

````markdown
# {機能名}（作業メモ）

> 実装完了時に削除する一時メモ。恒久情報は`{機能名}.md`へ転記する。

## 現在のフェーズ

Phase: {1, 2, 3a, 3b, 3c, 4, 5のいずれか}

## 既存調査メモ

（Exploreフェーズでspec-researcherから集約した調査結果）

### {観点1}

-
-

### {観点2}

-
-

## タスク分解

（Tasksフェーズで分解した実装タスク。TaskCreate登録と同期）

- [ ] T1:
- [ ] T2:
- [ ] T3:

## レビュー履歴

（codexレビューや進行中の方針変更メモ）

-
-

## 未決事項

（ユーザー確認待ちや仮置きの方針。確定したら本節から削除する）

-
-
````

## レビュアー呼び出し（spec-reviewer・code-quality-reviewer）

Phase 5で機能単位に直列起動するレビュアーの呼び出しテンプレート。
両レビュアーに共通する引数は「機能名・恒久ドキュメントのパス・`BASE_SHA`・レビュー対象外の一時ファイル一覧」の4点。

`BASE_SHA`はPhase 5開始時点の`git rev-parse HEAD`の結果を使う。
spec-implementerはタスク単位でコミットしない設計のため、レビュー時点の差分は未コミットの作業ツリー差分として扱う。
差分取得は`git diff {BASE_SHA}`と未追跡ファイルを含む作業ツリー参照（`git status`・`Read`）で行う。
`docs/v{version}/{機能名}.working.md`などの一時ファイルは実装完了時に削除される想定のため、評価対象外として除外する。

### spec-reviewer

```text
以下の機能について仕様適合性レビューを実施してください。

機能名: {機能名}
恒久ドキュメント: `docs/v{version}/{機能名}.md`
BASE_SHA: {Phase 5開始時点のHEAD}
対象外ファイル: docs/v{version}/{機能名}.working.md

差分取得: `git diff {BASE_SHA}`と、未追跡ファイルを含む作業ツリー参照で行うこと
制約: 読み取り専用。実装者レポートは鵜呑みにせず、コードと差分で独立検証すること
出力: 仕様適合（✅）または指摘（❌、file:line形式）
```

### code-quality-reviewer

```text
以下の機能についてコード品質レビューを実施してください。

機能名: {機能名}
恒久ドキュメント: `docs/v{version}/{機能名}.md`
BASE_SHA: {Phase 5開始時点のHEAD}
対象外ファイル: docs/v{version}/{機能名}.working.md

差分取得: `git diff {BASE_SHA}`と、未追跡ファイルを含む作業ツリー参照で行うこと
制約: 読み取り専用。coding-standardsスキルを事前に呼び出し、品質基準に従うこと
出力: Strengths / Issues (Critical/Important/Minor) / Assessment (approve/reject)
```

## 参照コメント（恒久`.md`への1行参照）

言語別の書式例のみ示す。配置・対象範囲・`.working.md`を参照しない理由はSKILL.md「参照コメント方針」節を参照する。

```python
# 仕様: docs/v1.2/ユーザー認証.md
```

```typescript
// 仕様: docs/v1.2/ユーザー認証.md
```

```rust
// 仕様: docs/v1.2/ユーザー認証.md
```

```csharp
// 仕様: docs/v1.2/ユーザー認証.md
```

```powershell
# 仕様: docs/v1.2/ユーザー認証.md
```
