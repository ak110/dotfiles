---
name: plan-mode
description: >
  計画ファイル作成～実装プロセスを強化するスキル。
  Claude Codeがplan modeに入った時、または複雑な指示・多段階の作業を受けた時に最初に必ず呼び出すこと。
  単純な単一ファイルの修正指示や会話だけの質問には不要。
---

# 計画モード・計画ファイル作成

本スキルは、Claude Code標準のplan modeを強化し、より効率的かつ高品質な成果を得るための手順とガイドラインを提供する。

本スキルの利用時、エージェントは以下の手順に従い作業すること。

1. バグ対応手順
2. 計画ファイル作成前の要件対話
3. 計画ファイルの作成
4. 計画ファイルのcodexレビュー
5. 計画の実行

## 1. バグ対応手順（最優先で確認）

ユーザーの指示がバグ・障害・エラー・デグレードなどの対応に該当する場合、
他セクションを読む前に`references/bugfix-process.md`を読み込み、手順に従い確実に対応する。

再現の有無や発覚経路（CI失敗・テスト失敗・例外発生・挙動変化の報告など）は問わない。
該当する場合、本スキル呼び出し直後の最初のツール呼び出しは`Read`で`references/bugfix-process.md`を開くことに固定する。
それ以前に計画ファイルの作成や他セクションの解釈へ進んではならない。
この順序を守らないと、構成テンプレートを先に読んだ勢いで対症療法的な計画に陥りやすく、再発防止の検討が欠落する。

### バグ対応サマリーの記録

バグ対応時は計画ファイルの`## 背景`節末尾に以下の表を必ず含める。経緯・目的の本文を先に書き、その下に表を置く。
表形式で1か所に集約することで、別セッションへの引き継ぎと振り返りが容易になる。

| 項目 | 内容 |
| --- | --- |
| 根本原因 | 起点まで追跡した一次原因を1〜2文で記述 |
| 修正方針 | 直接の修正内容を1〜2文で記述（再発防止策と対で検討する） |
| 再発防止策 | 自動で機能する仕組み（テスト・CI・型制約など）を優先して記述 |
| 類似見直し結果 | 同パターンの他箇所を確認した範囲と結果 |
| 知見の記録 | 恒久化先（CLAUDE.md・ルール・スキル等）と転記内容 |

本表は対症療法ではなく根本対応に至ったことを示すチェックリストでもある。
`references/bugfix-process.md`の手順で得た結論を本表に集約する。
「類似見直し結果」欄は計画ファイル作成前に類似箇所の調査を完了させ、確定した結果を記入する。
未調査のまま「調査予定」などの保留表現で残したまま計画ファイル提示に進んではならない。

## 2. 計画ファイル作成前の要件対話

ユーザー指示が曖昧・複数の解釈が成り立つ場合は、そのまま計画ファイル作成へ進んではならない。
先に選択肢を提示など、要件を対話的に明確化してから計画ファイル作成に進む。

曖昧さを残したまま骨子を書き始めると、計画ファイルへの追記とユーザー確認が多ターンに分散する。
その結果、トークン効率と最終的な計画品質の両方が低下するため。

- 対話で確定させる観点は以下に絞る（実装詳細には立ち入らない）
  - 目的・ゴール
  - 制約・前提
  - 成功条件
  - スコープ境界
  - 方針の選択肢
- 推奨手段は`AskUserQuestion`
  - 複数の論点はバッチでまとめて提示し、多ターンの段階的プロンプトを避ける（やり取りが分散するとユーザー側の負担も増えるため）
- 対話で得た結論は、計画ファイル作成時に以下のセクションへ転記する。エージェントの推測判断と混ぜない
  - `## 背景`（経緯と目的）
  - `## 対応方針 > ユーザー合意済み事項`（合意した方針）

## 3. 計画ファイルの作成

### セルフレビュー実施判定

実装・検証後のセルフレビューは、既定では行わない。
以下のいずれかに該当する場合のみ行うこととする。

- ユーザー指示内に`セルフレビュー`の指示がある場合
 （「しっかりレビュー」「品質重視で」等の自然言語表現は採用合図として扱わない。誤起動を避けるため）
- 明示的な指示が無くとも大規模計画と判断される場合は`AskUserQuestion`で実施可否を確認する
  - 大規模判定の目安: 計画ファイルが複数フェーズ分割を要する規模、
    または公開インターフェース変更を伴う複数ファイル横断の設計変更。
  - 軽微な修正（誤字・単一関数修正・既存パターン踏襲）では確認もスキップし不実施とする。

### 参照テンプレート読み込み

計画ファイルの骨子を書き始める直前に、`references/plan-file.md`を必ずReadで読み込む。
テンプレートには全セクションの記述例に加え、計画ファイルの構成・セクション別の注意点・
レビュー省略可否の判断基準・複数フェーズ計画の骨子例を収録している。
`plan-file.md`は内容量が多いため、参照時点で読み込むことで要件対話中の不要な負荷を避ける。

## 4. 計画ファイルのcodexレビュー

ユーザーに計画ファイルを提示する前に、codexでレビューする。
plan modeの制約下でもcodexレビューは制約対象外として扱える（計画ファイル自体のレビューのため）。

codexレビューは計画ファイル自体に対するレビュー工程であり、
計画ファイルの`## 実行方法`節のセルフレビューとは別物である。
codexの実行状況・スキップ判断・指摘対応履歴を計画ファイル本文へ記載する必要はなく、メインとユーザー間のやり取りで完結させる。

### MCPツール版（優先）

`mcp__codex__codex` / `mcp__codex__codex-reply` が利用可能なら優先して使う。

- 初回: `mcp__codex__codex` を以下のパラメーターで呼び出す
  - `cwd`: `"{project_directory}"`
  - `sandbox`: `"danger-full-access"`
  - `prompt`: 以下の文言をそのまま使う（CLIフォールバック時も同一文言を使用する）

    ```text
    {plan_full_path} この計画ファイルをレビューして。
    以下の手順で行うこと。
    1. 考えられる指摘候補を内部的に網羅列挙する
    2. そこから確実に問題である致命的かつ本質的な欠陥のみを残し最終応答とする
    3. 今回の応答で全て出し切り、再レビューで追加の新規指摘を出さない

    些末な指摘、不要な提案、推測による指摘は禁止。
    計画ファイルの記述スタイル（構成・表現・書き方）に対する指摘も禁止。（記述間の矛盾は除く）
    実装時にエージェントが判断可能な細部（変数名・エラーメッセージ文言・ループ構造・小規模な制御フローなど）への指摘は禁止。
    `agent-toolkit:*`スキルや`careful-*-reviewer`などの識別子はClaude Code側の仕組みでcodex側からは存在確認できないため、これらの実在性に関する指摘は不要。
    指摘する場合は修正方針も簡潔に併記すること。
    指摘が無い場合は指摘無しと応答すること。
    ```

- 2回目以降: `mcp__codex__codex-reply` を以下のパラメーターで呼び出す
  - `threadId`: 前回の戻り値から取得したthreadId
  - `prompt`: `計画ファイルを更新したので再レビューして。`

### CLIフォールバック版

MCPツールが利用できない場合はcodex CLIを試す。
CLIの詳細コマンドは `references/codex-cli.md` を参照。

### どちらも使えない場合

MCPもCLIも使えない場合は、計画ファイル提示前にユーザーに状況を報告し、以下の選択を仰ぐ。

- codex CLIを別途セットアップし、codex MCPをインストールしてやり直す（推奨）
  - インストールコマンド: `claude mcp add --scope=user codex codex mcp-server`
- codex CLIを別途セットアップしてやり直す
- 当該環境ではcodexレビューを永続的にスキップする旨をプロジェクトの `CLAUDE.local.md` に記載してスキップする
  - 当該ファイルはgitignore推奨

### codexレビューの進め方

- ユーザーへの確認事項がある場合は必ずcodexレビュー前にユーザーに確認し結論を確定させておく
  - ただし、方針レベルの合意を実装詳細レベルの合意と誤解しないよう注意
- codexレビューでユーザー判断な指摘があった場合、その場ですぐにユーザーに確認して結論を確定させる
- codexの指摘がなくなるまで更新とレビューを繰り返す
  - ただし、合否判断は呼び元が行ってよい（指摘が的外れなものしかない場合なども合格扱いでよい）
- 一度指摘ゼロになった後に限り、その後の軽微な修正では再レビューを省略してよい
  - 方針レベルの変更時は再レビューする
- codexの指摘に基づいて計画ファイルを修正する際は、どのような修正をするかユーザーに伝える

## 5. 計画の実行

セルフレビュー実施時は、メインから`agent-toolkit:careful-impl`スキルを呼び出してその後の工程を実施する。

セルフレビュー不実施時は、メインが`## 実行方法`に従って実装・検証・コミットを行う。

## Gotchas

- 要件が固まる前に計画ファイルの骨子を書き始めがち
  - 曖昧さが残る段階では`AskUserQuestion`などで選択肢を確定させてから計画ファイル作成に進む
- 方針レベルの合意を実装詳細レベルの合意と取り違えやすい。ユーザーに確認するときは粒度（方針か実装詳細か）を明示する
- 計画ファイルの`## 変更内容`にコード修正内容をコードブロックで転記しがち
  - コード変更は方針・境界・挙動・外部インターフェース・確定済みの制約のみを書き、
    変数名・制御フロー・エラーメッセージ文言などは実装エージェントの裁量に任せる
- ドキュメント修正の場合に計画ファイルに抽象的な方針だけを書きがち
  - ドキュメントは変更後の文面そのものをコードブロックで示すことで評価可能性が高まる
  - 配置先の判断基準は`references/plan-file.md`の「セクションごとの注意点」に集約してある
- `## 背景`・`## 対応方針`に関数名・クラス名などプログラムレベルの識別子を書きがち
  - これらの節は判断材料を提示する場であり、実装詳細は`## 調査結果`・`## 変更内容`に分離する
  - 詳細は`references/plan-file.md`の「セクションごとの注意点」の対応項目を参照
