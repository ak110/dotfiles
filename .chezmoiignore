.github/**
CLAUDE.md
LICENSE
README.md
deploy.sh
docs/**
install.sh
requirements.txt
share/**
update.sh

# Windows: Linux専用ファイルをスキップ
{{ if eq .chezmoi.os "windows" }}
.bash_profile
.bashrc
.inputrc
.screenrc
.tmux.conf
{{ end }}

# Linux: Windows専用ファイルをスキップ
{{ if ne .chezmoi.os "windows" }}
bin/update-dotfiles.cmd
bin/update-ssh-config.cmd
{{ end }}

# run scripts: OS別に除外（ターゲットパスはプレフィックス除去後の名前）
{{ if eq .chezmoi.os "windows" }}
ssh-config.sh
{{ end }}
{{ if ne .chezmoi.os "windows" }}
ssh-config-windows.ps1
{{ end }}
