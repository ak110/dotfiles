{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"& { uv run --script $env:USERPROFILE\\dotfiles\\scripts\\claude_hook_pretooluse.py; if ($LASTEXITCODE -eq 2) { exit 2 } else { exit 0 } }\""
          }
        ]
      }
    ]
  }
}
